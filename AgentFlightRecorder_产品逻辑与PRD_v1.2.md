# Agent Flight Recorder：产品逻辑与 PRD v1.2

> 定位：面向 Agent 支付平台与保险机构的可验证事故证据、理赔判断与资金恢复基础设施，支持证据包导出、脱离平台独立验证与证据摘要链上存证。
>
> 一句话：Payments let agents spend. We make their mistakes recoverable.

**文档日期：** 2026-09-05\
**状态：** UPDATED DRAFT｜Review 修订版；§20 三个解封 Spike 全部通过前不得进入 Technical Spec\
**阶段：** 黑客松原型，同时保留创业落地路径\
**实现状态：** NOT STARTED / NOT VERIFIED；本文描述目标与验收要求，不代表代码已经完成\
**主赛道建议：** C｜可验证的 AI 推理审计，关联 D｜AI × Web3 / Fintech；B 为辅助关联方向。

**v1.2 变更：** 依据只读 Review 修订：①包承诺改为两阶段构造，闭合 manifest 文件清单与 anchors.json 之间的循环依赖；②"授权先于付款"改为显式区块高度规则（默认确认数 N=1，目标链为即时最终性），并补充存证失败/超时与支付路径的耦合；③独立验证器信任配置随其开源仓库版本化发布，拒绝包内信任材料；④执行环境证明的 CLI 验证收缩为签名与程序哈希白名单匹配，并新增验证结论上限；⑤P0 存证收缩为授权承诺与最终包承诺两点，逐版本存证与 quote 级 attestation 验证降为 P1；⑥统一验证四态（Pass / Fail / Unknown / Not Present）与 Manifest 状态映射；⑦补充锚定幂等、密钥轮换注册表、RFC 8785（JCS）规范化、Superseded 过期版本检测；⑧计价措辞统一为明确标记的测试资产；⑨新增技术基线（Injective EVM 测试网 1439、legacy 交易与显式 gas、RPC 可配置与归档选项、主网 1776 保持 NOT DEPLOYED）与三个解封 Spike；⑩执行环境证明改为软件级签名（不假设官方 CDE，前后端自托管部署），硬件 TEE 降为 P1，包格式与验证器接口不变。原 v1、v1.1 保留供比较，所有能力仍为 NOT STARTED / NOT VERIFIED。

---

# 第一部分：项目完整逻辑

## 1. 我们观察到的变化

AI Agent 正从“给建议的软件”变成“可以产生经济后果的软件”。它开始自主购买 API、算力、数据、广告、订阅与其他机器服务，并逐步进入企业采购、退款和供应商付款。

支付协议正在补齐前半段：

- x402 提供机器原生的支付与结算；
- AP2 用 Mandate 表达用户意图与支付授权；
- Mastercard Verifiable Intent 将用户身份、授权约束和交易结果绑定；
- Agent Wallet、Token 与支付网关负责保管凭证、限额和执行。

这些技术已经覆盖部分争议证据：AP2 的 Mandate/Receipt、Mastercard VI 的 identity-intent-action 绑定，以及 t54 的风险判断、可验证审计链和案件导出，都在让 Agent 交易更容易复核。它们尚未共同形成一个跨支付轨道、由保险运营方拥有规则的完整闭环：

> 一笔技术上合法、已经完成结算的交易，如果事实上是 Agent 花错的钱，如何把已有授权与支付证据、结算后的执行上下文、净损失、保障规则、人工处置和恢复连接起来？

## 2. 用户现在面对的责任真空

一次 Agent 支付事故的信息分散在多个主体手中：

1. 用户或企业保存原始任务与授权；
2. Agent 平台保存模型调用和部分执行轨迹；
3. MCP 或工具提供者保存工具请求；
4. 钱包保存签名与广播信息；
5. 区块链保存最终交易；
6. 商户保存商品、交付与退款记录。

每个系统只知道一部分事实。传统日志还可能被拥有系统的一方修改、删除或选择性披露。因此，事故发生后很难可靠回答：

- 用户究竟授权了什么？
- Agent 看到的哪些外部内容影响了支付？
- 支付参数在何处产生或改变？
- 实际交易是否偏离授权？
- 偏离来自攻击、模型异常、工具故障还是用户本人？
- 是否发生了可赔偿的实际损失？
- 哪一条保单责任或除外条款适用？

这使保险方难以快速完成一致的事故调查，支付平台难以公平处理跨系统争议，企业也不敢提高 Agent 的自主权和支付额度。这里不声称现有系统“完全无法理赔”，而是认为其调查材料仍然分散、缺少统一案件对象与 Agent 特有的事故语义。

## 3. 我们的产品判断

Agent Flight Recorder 不试图消灭全部事故。它接受一个现实：安全措施会降低事故率，但无法把事故率降为零。

我们在安全失效之后提供三项能力：

1. **证明事实：** 将事前授权、关键执行行为和最终交易绑定成可验证证据。
2. **辅助归责：** 将事故归类，比较授权与实际行为，解释哪些条款可能适用。
3. **执行恢复：** 在人工授权后，将已批准的理赔结果交给测试赔付合约或现实保险系统。

因此，我们不是另一个 Agent 安全扫描器，也不是一家无牌照保险公司。我们是支付安全层和保险承保层之间的 recovery and claims layer。

## 4. 核心产品对象：Verifiable Claim Packet

系统为每一次受保护的 Agent 支付建立统一关联标识，并在事故后输出 Claim Packet：

- Claim ID、Payment ID、Agent Run ID；
- 用户或企业签署的 Mandate；
- 当时生效的保障条款与策略版本；
- Agent、模型、工具和执行环境版本；
- 影响付款的关键输入来源、摘要或哈希；
- 支付工具请求及结果；
- 金额、币种、收款人、用途和有效期；
- 钱包签名与链上交易回执；
- 关键事件签名与哈希关联，以及存在时的 Merkle proof、链上锚定与 TEE attestation；
- 可量化损失；
- 确定性规则检查结果；
- AI 事故分类、条款映射、置信度与证据引用；
- 人工决定、理由、时间与批准者；
- 最终赔付或拒赔结果。

Claim Packet 的价值不在于保存更多日志，而在于让不同主体可以围绕同一组已声明来源、完整性状态与限制条件的材料作出可复核决定。密码学只能证明签名或锚定后的内容未被无痕修改，不能自动证明外部内容真实，也不能单独证明事故因果关系。

### 4.1 Evidence Manifest最低字段

每个进入 Claim Packet 的关键证据至少包含：

- `evidence_id`：案件内唯一标识；
- `producer`：用户、Agent Runtime、Gateway、钱包、链或商户；
- `schema_version`：规范版本；
- `content_hash`：规范化内容哈希；
- `parent_hash`：前一关键事件哈希；
- `signature` 与 `key_id`：生产者签名及其密钥标识；
- `observed_at`：记录时间；
- `anchor_reference`：可选的链上锚定或可信时间引用；
- `privacy_class`：公开、受限或仅可在可信环境处理；
- `verification_status`：有效、无效、缺失或无法验证（与验证器输出映射：有效=Pass、无效=Fail、无法验证=Unknown、缺失=Not Present；UI 与 CLI 一律使用四态标签）。

Mandate 必须在支付前完成签名或可信时间固定。系统必须保留验证程序哈希（软件级为可复现构建哈希，硬件 TEE 时为 measurement）、验证所依赖的信任根和密钥轮换记录。执行环境证明对象带 type 字段（software / tee）：P0 使用 software，由独立验证者密钥对程序哈希、输入摘要与输出摘要签名；P1 引入硬件 TEE 时不改变包格式与验证器接口。平台在首次可信时间固定之前仍可能构造一条自洽记录，因此产品不得把“哈希链连续”夸大为“事实一定真实”。

### 4.2 可导出的证据包：把事故卷宗交到用户手中

审核员或获授权用户可以导出一个版本化 ZIP 证据包。取得证据包后，即使平台网站不可访问，也能用独立验证工具检查手中的材料。P0 不承诺平台停止服务后仍可获取尚未导出的材料、继续审核或完成赔付。

证据包至少包含：

| 文件或目录 | 内容与用途 |
|---|---|
| `manifest.json` | 包格式版本、随机包标识、案件和支付关联、文件清单、各文件摘要、披露范围、包版本、前版承诺 |
| `mandate.json` | 付款前已签署的授权及对应签名材料 |
| `evidence/` | 本次获准披露的关键输入、工具调用、交付结果与交易关联材料 |
| `analysis.json` | AI 输入摘要、输出、模型与提示版本、真实证据引用、局限性 |
| `decision.json` | 存在时的人工作出的决定、决定版本和理由；尚未决定时明确标为不存在 |
| `proofs/` | Manifest 签名、执行环境证明（软件级；存在时含 TEE attestation）、必要验证材料及内容承诺的随机盐 |
| `anchors.json` | 授权存证与包存证的链 ID、合约、交易、区块及承诺引用（不纳入包承诺的文件清单，构造顺序见 §4.4） |
| `README.md` | 独立验证步骤、隐私说明、信任假设和缺失项解释 |

包导出前展示披露范围与接收对象，只导出当前操作者有权取得的材料。公开版不得夹带受限原文；不同披露集合使用各自明确标记的 Manifest 和承诺。公开版通过验证不等于完整案件通过验证。

已导出的包不可原地改写。补证、AI 重分析、人工决定或退款信息更新均生成新包版本，绑定前版承诺并重新签名；P0 只对最终包版本做链上存证，逐版本存证属于 P1（见 §20）。审核前包可以没有最终决定，最终归档包必须明确其对应的决定与赔付状态。

### 4.3 独立验证工具：不让平台替自己打勾

提供开源命令行验证器，作为 P0 必须交付物；平台控制台提供同类结果展示。验证器无需登录平台、调用平台后端或调用 AI 模型。

验证分为两个模式：

- 离线检查：安全读取包、重算文件摘要、检查 Manifest 签名和对象关联、列出缺失证据。仅有包内自带公钥时，最多确认签名在数学上匹配，签名者身份仍为 Unknown。
- 在线检查：使用验证者自己配置的测试网 RPC、可信合约地址与签名者信任材料，进一步验证链上承诺、交易成功状态、确认数与版本新鲜度。仅从包内读到的回执或浏览器链接不能代替链上核验。执行环境证明验证其签名是否来自信任配置中的验证者密钥，以及程序哈希是否在白名单内；硬件 TEE attestation 项在 P0 显示 Not Present，quote 级密码学验证属于 P1。在线模式发现链上同一引用标识存在更新版本时，输出 Superseded 提示，不改变逐项 Pass/Fail 结果。

信任配置随验证器开源仓库版本化发布（trusted_signers.json：key_id、公钥、生效窗口、EvidenceAnchor 合约地址、链 ID、预期验证程序哈希），验证器默认从仓库内固定路径读取，并支持命令行参数覆盖。信任配置必须经此包外渠道取得：验证器应拒绝被包内配置覆盖的信任根，不能因为包中附了一把公钥就认定来源可信；信任配置的真实性等同于该仓库发布的真实性，平台后端不参与分发。链上查询依赖所选 RPC 和链的安全假设；P0 不声称使用无信任轻客户端。

验证结果逐项输出 `Pass / Fail / Unknown / Not Present`，覆盖内容完整性、签名者信任、对象关联、授权先后顺序、链上存证、支付与赔付回执、执行环境、披露完整程度。关键检查未知或缺失时，总体不得显示“全部验证通过”。

验证结论上限：内容完整性、签名数学匹配、链上承诺一致性、回执状态最高可达 Pass；签名者身份在无信任配置时为 Unknown；来源真实性与事故因果关系不由验证器判定，最高表述为“与已锚定证据一致”；AI 判断正确性与人工赔付决定的正确性不属于验证范围，验证器只确认其存在、签名与版本。UI 与 CLI 均不得超出上述上限表述。哈希匹配不能验证 AI 判断的正确性，也不能证明未披露材料不存在。

链上核验实现约束：确认数以显式区块高度差计算，不使用 finalized/safe 等 block tag；核对承诺优先使用 eth_call 读取合约存储；回执查询失败或 RPC 故障时对应项输出 Unknown，不得显示通过。

验证器对 ZIP 路径穿越、重复文件路径、超限文件、重复 JSON 键等输入拒绝处理；不执行包内脚本或任意网络地址。原始包应验证成功，修改关键证据、删除必需文件、替换未知签名者、伪造链 ID 的样本应得到对应失败或未知结果；以旧版本冒充最新的样本应得到 Superseded 提示。

### 4.4 证据摘要链上存证：登记可核对的内容指纹

P0 在 Injective EVM 测试网部署最小 EvidenceAnchor 合约，只登记内容承诺、随机引用标识、版本和提交者，并产生可查询事件。原始用户指令、外部网页、模型输入、案件原文与可识别个人信息均不上链。链上仍会暴露提交地址、交易时间和存证频率，不能声称没有元数据泄露。

存证采用两个时点（P0 仅锚这两点，逐版本存证属于 P1）：

1. 授权存证：付款前，对已签署 Mandate 形成承诺并完成约定确认数。“授权先于付款”的判定规则为：block_number（授权存证交易）+ N ≤ block_number（支付交易），N 为可配置确认数，P0 默认 N=1（目标链为即时最终性，1 个确认即最终）；同一区块内的授权与付款视为不满足。本地时间戳不足以替代链上顺序。Gateway 只在规则满足后发起受保护付款；存证 Failed 或超过可配置超时（默认 60 秒）时，该支付路径终止并标记 Unprotected，不得以重试方式转为受保护支付。
2. 证据包存证：付款结算与人工决定完成后，对最终版本的 Manifest 形成承诺，再导出附带锚定引用的包。它支持该版本内容与已确认承诺的一致性，不追溯证明每条材料在事故前已存在。

包承诺按两阶段构造，闭合文件清单与锚定引用之间的循环：阶段一生成不含 anchors.json 的 Manifest，计算包承诺并完成链上存证；阶段二写入 anchors.json（含本包存证引用）后导出。manifest.json 的文件清单必须排除 anchors.json；验证器对 anchors.json 不做摘要核验，改为按其中引用逐项查询链上并与包承诺比对。偏离此构造顺序的包视为格式错误。

实施时固定 UTF-8、RFC 8785（JCS）JSON 规范化、字段类型和算法版本（SHA-256），并随仓库给出跨环境一致的测试向量。逻辑上的包承诺为 `SHA-256(域标识 || 随机盐 || 规范化Manifest字节)`，各段边界必须无歧义；使用不少于 128 位、来自 CSPRNG 的高熵随机盐，降低低熵内容被猜测的风险。Manifest 摘要覆盖本次披露内容；自身签名、链上回执与包承诺不纳入自身摘要，避免循环依赖。ZIP 压缩时间等容器属性不作为内容身份。

对（同一提交者、随机引用标识、版本）写入相同承诺必须幂等成功，写入不同承诺必须拒绝；更新必须升版，不覆盖旧记录。包盐及受限材料仅向相应验证者披露，第三方不得从公开承诺推导未授权原文。

存证状态与支付、案件状态分开：`Not Submitted / Pending / Confirmed / Failed`。只有实际查询链上成功事件且达到配置确认数，才能显示 Confirmed；链 ID、合约、提交者、版本和承诺必须逐项匹配。确认数一律以显式区块高度差计算，不依赖 finalized/safe 等 block tag；核对承诺优先使用 eth_call 读取合约存储，回执查询失败时对应项输出 Unknown。目标链为即时最终性，重组重估属于休眠的通用要求而非验收重点；无法查询时重新评估状态，不显示历史缓存的绿色结果。

## 5. 受保护交易的完整生命周期

### 5.1 创建保障与授权

用户或企业在 Agent 执行前确认：

- 允许执行的任务；
- 总预算和单笔上限；
- 币种；
- 允许或禁止的商户/收款人；
- 有效期；
- 允许调用的支付工具；
- 是否需要人工二次确认；
- 适用的保障条款与除外责任。

V1.2 生成本项目格式的签名 Mandate（EIP-712，domain.chainId 使用 EVM 数字 ID 1439），并在 P0 测试网完成付款前授权承诺存证与确认；未确认时不得开始这条受保护支付路径。创业阶段如获得 AP2 或 Mastercard VI 的正式接入条件，再通过适配器引用其授权材料，不重新定义上游协议。

### 5.2 Agent执行任务

Agent 可以正常搜索、检索、推理和调用工具。系统不保存完整 chain-of-thought，也不要求所有模型推理运行在 TEE 中。

只记录会影响责任判断的关键事件：

- 可信指令与外部数据的来源；
- 授权策略版本；
- 高风险工具调用；
- 支付请求参数；
- 执行结果与错误；
- 关键事件之间的顺序和关联。

### 5.3 强制支付经过Evidence Gateway

Agent 不直接持有可以绕过记录器的无限制支付能力。所有受保护支付必须经过 Evidence Gateway：

1. 接收 Agent 的支付请求；
2. 关联 Mandate 与当前运行；
3. 执行金额、收款人、时间和权限等硬约束检查；
4. 形成规范化证据事件并签名；
5. 再调用钱包或支付轨道完成交易；
6. 获取交易回执并结束证据链。

如果 Agent 可以绕过 Gateway 直接签名，系统必须明确标记该交易不受保护，而不是伪称证据完整。

### 5.4 事故发生与申报

事故可由以下主体发起：

- 用户主动报告；
- Agent 平台监控发现；
- 支付网关发现重复、异常或越权支付；
- 保险方或企业运营人员导入争议。

创建 Claim 时，系统首先确认交易已结算、存在损失且证据可定位。交易回滚或支付失败但未造成损失时，不进入赔付流程。

黑客松使用以下净损失口径：

```text
eligible_loss = min(
  已结算且被认定属于承保事故的金额 - 商户退款 - 已追回资金,
  演示保障上限
)
```

`eligible_loss` 小于或等于零时不得赔付。生产环境的币价、手续费、部分履约、追偿顺序和免赔额由持牌合作方的正式条款决定，本项目不自行解释。

### 5.5 证据验证与AI理赔分析

系统按固定顺序处理：

1. 验证签名、事件关联，以及当前案件实际提供的 Merkle proof、链上锚定和执行环境证明（含存在时的 TEE attestation）；
2. 比较 Mandate 与实际支付的硬字段；
3. AI 识别可能的事故原因；
4. AI 将证据映射到保障条款；
5. AI 列出缺失证据、矛盾和置信度；
6. 输出带逐项引用的理赔建议。

AI 输出必须是结构化对象，至少包含：

- `primary_hypothesis`：主要事故假设，而不是已被证明的事实；
- `candidate_causes`：其他可能原因及支持/反对证据；
- `evidence_refs`：每个结论引用的 Evidence ID；
- `policy_refs`：可能适用的保障条款或除外责任；
- `missing_evidence` 与 `contradictions`；
- `confidence_band`：高、中、低，不伪造精确概率；
- `recommended_next_state`：人工审核、补证或拒绝进入处理；
- `limitations`：模型与材料限制。

模型只能读取完成最小披露和脱敏后的必要证据片段；哈希用于完整性验证，不能替代模型进行语义分析所需的内容。AI 的输出是可审查假设，不是密码学证明，也不能覆盖确定性验证结果。

AI 不是最终裁判。低置信度、证据矛盾或高额 Claim 必须升级人工审核。

### 5.6 人工决定与资金恢复

黑客松中的演示审核员可以：

- 批准全额赔付；
- 批准部分赔付；
- 拒赔；
- 要求补充证据；
- 标记疑似欺诈；
- 将争议交给更高权限人员。

黑客松版本由 ClaimVault 在测试网执行测试资产恢复。每次执行使用 `claim_id + decision_version` 作为幂等键；交易进入最终确认后才能标记 `Paid`，失败则进入 `Payout Failed` 并保留安全重试入口。

创业版本中，保单、客户沟通、保障判断和真实赔付必须由当地获许可的保险公司/MGA或明确合法的企业自保主体拥有。Agent Flight Recorder 只提供证据与案件工作流，不托管真实赔付资金。进入任何司法辖区、接入真实保单或采用按理赔金额分成之前，都必须经过当地法律与合规 Gate。

## 6. V1保障边界

### 6.1 可进入理赔判断的事故

V1 聚焦“Agent 支付执行偏离事前授权”且造成实际损失的情形：

- 金额超过 Mandate 上限；
- 支付给未授权收款人；
- 重复支付；
- 授权过期后支付；
- 调用未授权支付工具；
- 金额、时间等硬约束虽然通过，但 Agent 因间接提示词攻击购买了与用户任务语义不符的对象或服务；
- Agent 因间接提示词攻击产生其他可证明的授权偏离；
- Agent 或工具故障导致以上可验证偏离。

生产环境中，可由硬规则明确识别的越权支付应优先被阻止，而不是故意放行后等待理赔。理赔层主要覆盖的是安全控制遗漏、配置失效，以及形式上合规但语义上偏离用户真实任务的残余风险。

### 6.2 V1明确不承诺保障

- 用户在清晰看到金额、币种和收款人后明确签署的具体交易；
- 投资亏损、价格波动或机会成本；
- 买家后悔、商品质量争议或普通商户服务纠纷；
- Agent 建议不佳但用户自主完成的交易；
- Gateway 之外的钱包失窃或私钥泄露；
- 没有形成完整证据、无法证明与受保护运行相关的交易；
- 违法用途、制裁规避或其他保障条款禁止事项；
- 真实保险赔付承诺。

### 6.3 关于“人类被AI误导”

如果人类只签署了带约束的高层目标，Agent 随后编造事实并执行超出目标的交易，可按 Agent 执行偏离调查。

如果人类已经看到并明确批准最终交易，则 V1 默认不赔。AI 建议错误、错误投资推荐与销售误导属于更复杂的 AI E&O 风险，放入未来版本，不与本次黑客松闭环混合。

## 7. 为什么必须同时使用规则、AI与人工

### 确定性规则负责

- 金额是否超限；
- 收款人是否允许；
- 授权是否过期；
- 是否重复支付；
- 签名和证明是否有效；
- 链上是否发生结算。

### AI负责

- 识别间接提示词攻击或语义劫持；
- 将非结构化行为整理成事故时间线；
- 在多项证据中定位矛盾与缺口；
- 将事故事实映射到自然语言条款；
- 生成带引用、置信度和局限性的理赔报告。

### 人工负责

- 最终赔付或拒赔决定；
- 处理低置信度和灰色因果关系；
- 审查疑似欺诈；
- 对外承担业务、合规与资金责任。

去掉 AI，系统会退化成字段比较和日志查看器；去掉确定性验证，AI 报告不可靠；去掉人工，系统会变成不负责任的自动赔付工具。

## 8. 为什么需要TEE和链上证明

TEE 证明指定版本的证据处理或理赔程序在声明的可信环境中运行，并保护敏感输入。链上锚定证明某批证据在某个时间已经存在，之后没有被无痕替换。

二者都不能证明：

- Agent 的语义判断必然正确；
- 外部输入本身真实；
- AI 的事故分类永远正确；
- 硬件不存在任何漏洞。

所以产品只声称“证据处理过程可验证、记录篡改可发现”，不声称“完全去信任”或“全部 Agent 推理都可信”。

P0 的现实配置：不假设主办方或运行环境提供 CDE，演示前端、控制台与服务全部自托管部署；执行环境证明为软件级（独立验证者密钥签名），信任根是验证者密钥而非硬件，UI 与包内必须如实标注；硬件 TEE 属于 P1。本节论证的是 TEE 的价值与上限，不代表 P0 已具备硬件能力。

### 8.1 去中心化边界

本方案的付款入口、材料保管、AI 调用和人工审核仍由明确运营主体控制。链上存证与测试合约执行复用区块链能力；已取得证据包的获授权第三方可以在平台之外独立核验。这降低了对平台展示结果的依赖，但不构成去中心化仲裁、去中心化存储或无信任保险。

P0 不增加 DAO、治理代币、IPFS、多方投票赔付或去中心化模型网络。衡量目标是“第三方能否验证持有的材料”，而不是堆叠去中心化名词。平台停机后可验证性以用户已持有包、可用验证器及必要外部信任材料为前提。

## 9. 创业商业逻辑

### 9.1 初始买方

首个理想客户明确收窄为：**Agent Wallet 或 Agent 支付网关的风险/争议负责人**。他们已经控制支付入口，也直接承担客户事故调查、退款协调和信任成本，是最容易接入 Evidence Gateway 的一方。

保险公司/MGA首先作为条款与 Claim Packet 的设计伙伴，待案件量和损失数据形成后再成为承保与分销伙伴。后续扩展买方包括：

- x402 / 稳定币支付服务商；
- 部署采购、退款或付款 Agent 的企业；
- AI Agent 平台；
- AI 保险 MGA、保险公司与再保险机构。

### 9.2 它们购买什么

- 让客户敢于为 Agent 设置更高额度；
- 降低事故调查与争议处理成本；
- 获得可用于保险承保的证据；
- 通过企业采购、风控和合规审查；
- 将不可逆支付转化成存在恢复路径的支付。

### 9.3 收费方式

早期不承担保险风险，收入来自：

- 平台订阅；
- 按受保护交易量或证据事件量收费；
- 每次 Claim Packet / 理赔处理收费；
- 企业级审计、保留和导出能力；
- 通过法律与牌照审查后，与持牌承保方合作的技术服务费或合规允许的收入分成。

### 9.4 长期数据飞轮

受保护交易越多，获得的真实事故类型、损失金额和控制效果越多；理赔判断和风险分层越准确；保险方越愿意提供更高额度和更低价格；更多 Agent 平台因此接入。

真正的长期资产不是日志数量，而是经过结果验证的“Agent 行为—事故原因—实际损失—最终决定”数据。

## 10. 与现有玩家的关系

- **Mastercard VI / Google AP2：** 上游授权证据标准，黑客松仅参考数据模型，创业阶段再做正式适配；
- **x402：** 支付执行与结算轨道；
- **Langfuse / OpenTelemetry：** 调试和运行观测，可作为辅助数据源，不作为独立证据根；
- **Agent 安全产品：** 事前预防和实时阻止；
- **t54 等风险网关：** 已覆盖支付前风险、reasoning trace、可验证审计链、争议与案件导出，是直接相邻竞争者，也可能成为证据来源；
- **保险公司/MGA：** 负责承保、定价、合规与真实资金赔付；
- **Agent Flight Recorder：** 差异化收窄为结算后的执行上下文、净损失计算、由持牌方提供的保障规则、人工处置和恢复连接器，将上游材料转换成保险运营闭环。

### 10.1 P0要求与目标能力

下表是需求边界，不是完成清单。当前所有 P0 实现状态均为 `NOT VERIFIED`，只有真实运行和测试通过后才能改为 `IMPLEMENTED`。

| 能力 | 黑客松 P0 必须实现 | 数据模型兼容 | 创业目标 | 不得声称 |
|---|---|---|---|---|
| 本项目签名 Mandate | 完成一种本地签名流程 | 是 | 保留为本地/通用格式 | 已等同 AP2/VI 正式集成 |
| 测试网支付与回执 | 完成一种 EVM 流程 | 可映射 x402 字段 | 增加标准 x402 适配 | 支持全部 x402 Facilitator |
| AP2 Mandate/Receipt | 不集成 | 参考字段与语义 | 构建正式适配器 | 已获得 Google 生产接入 |
| Mastercard VI | 不集成 | 参考 layered intent/constraint 模型 | 在公开 API 可用且合作条件满足后接入 | 已接入 Mastercard Agent Pay |
| t54 | 不集成 | 理解其审计/案件对象 | 评估竞合与互操作 | 市场没有相邻证据产品 |
| 执行环境证明 | P0 为软件级：独立验证者密钥对程序哈希与输入输出摘要签名；硬件 TEE 为 P1 | 是 | 多运行环境与多信任根 | 全部 Agent 推理都在 TEE；软件级证明等同硬件 attestation |
| 可导出证据包 | 版本化 ZIP、披露清单与签名 Manifest | 是 | 跨机构交换与受控披露 | 导出后仍可撤回接收者的副本 |
| 独立验证工具 | 无平台后端依赖的开源 CLI，区分离线与在线验证；信任配置随 CLI 仓库发布；硬件 TEE attestation 显示 Not Present | 是 | 第三方集成与多信任根 | 未知签名者或离线回执等于可信 |
| 证据摘要链上存证 | Injective EVM 测试网的授权承诺与最终包承诺两点登记；逐版本存证为 P1 | 是 | 批量 Merkle 锚定与多链适配 | 上链等于事实真实或完全去中心化 |
| 真实保险赔付 | 不实现 | Claim 数据可导出 | 由持牌合作方拥有 | 本项目销售或承保保险 |

“Protocol-agnostic”是创业目标，不是黑客松当前完成状态。演示界面必须把 Implemented、Compatible Model、Planned 三种状态分开。

---

# 第二部分：产品需求文档（PRD）

## 11. Product Summary

Agent Flight Recorder 为受保护的 Agent 支付建立可验证执行记录，并在发生错误支付后生成可复核的 Claim Packet。系统用确定性验证确认授权偏离和证据完整性，用 AI 解释事故原因与条款适用性，由人工审核员作出最终决定，并在测试环境完成资金恢复。V1 使用的是公开标注的合成保障条款，不构成真实保单。

## 12. 产品目标

### 12.1 黑客松目标

在 3 分钟连续演示中证明以下闭环真实运行：

```text
用户授权
→ Agent受到提示词攻击
→ 错误支付完成
→ 证据完整性验证
→ AI事故归因与条款判断
→ 人工批准
→ 测试资产赔付
→ 证据包存证与导出
→ 独立验证原包与篡改包
```

（该闭环对应 §16 Core User Journey 步骤 1–14；§24 按时间轴排列同一链路，步骤编号以 §16 为准。）

同时演示一个不应赔付的对照案例，证明系统不是无条件兜底。

### 12.2 创业目标

成为支付协议无关的 Agent 事故证据和理赔 API，使支付平台与保险方能够为自主支付提供可承保的恢复保障。

## 13. 非目标

黑客松 V1 不做：

- 真实保险销售、承保或保费定价；
- 真实资金池或主网高价值赔付；
- 去中心化仲裁或概率市场；
- 全自动、不可申诉的理赔决定；
- 所有 Agent 框架和所有支付协议；
- 全模型推理进入 TEE；
- 投资建议、商品质量和普通商户纠纷保险；
- 生产级反洗钱、制裁筛查或保险牌照能力；
- 记录或展示模型私有 chain-of-thought。

## 14. Target Users

### 14.1 黑客松直接用户

**Agent 操作者：** 配置授权并让 Agent 通过测试资产执行购买。\
**受损用户/申报人：** 发现错误支付并查看可验证理赔状态。\
**演示审核员：** 查看证据时间线、AI 建议并对合成保障案例作出决定。\
**独立验证者/评委：** 验证证明、运行边界和链上结果。

### 14.2 创业买方

**首个买方：** Agent Wallet / 支付网关的风险与争议负责人。\
**设计伙伴：** 保险公司的数字风险承保/理赔负责人或 MGA。\
**后续用户：** Agent 产品负责人和企业财务自动化负责人。

## 15. 核心状态模型

### Payment状态

- Draft
- Authorized
- Recording
- Submitted
- Settled
- Reverted
- Failed（广播被拒或超过超时未被打包）

Payment 另有独立保护属性：Protected、Partially Protected、Unprotected。保护属性不得与结算生命周期混为同一状态。授权存证未达到 Confirmed 时，Payment 不得从 Authorized 进入 Submitted；存证 Failed 或超过可配置超时（默认 60 秒）时，该支付路径终止并将保护属性标记为 Unprotected，不得以重试方式转为受保护支付。锚定状态机见 §4.4，与 Payment、Claim 状态分开维护。

### Claim状态

- Created
- Evidence Collecting
- Evidence Verified
- Needs Information
- Under Review
- Approved
- Partially Approved
- Denied
- Payout Pending
- Payout Failed
- Paid
- Closed

### Evidence状态

- Complete and Verified
- Complete but Invalid
- Incomplete
- Conflicting
- Unavailable

## 16. Core User Journey

1. 用户打开产品，看到“正常场景”和“攻击场景”两个可运行任务。
2. 用户选择攻击场景，查看并签署任务 Mandate：“购买指定主题的有效风险报告，总价不超过 10 测试 USDC（明确标记的测试代币，不代表真实美元或真实 USDC）”。
3. 系统明确展示受保护条件：最高金额、服务类型、有效期和适用保障；该任务允许 Agent 在候选供应商中动态选择，因此没有预先固定单一收款地址。
4. Agent 开始购买 API/数据服务，关键事件出现在实时执行时间线中。
5. Agent 读取包含恶意指令的外部内容，被诱导购买攻击者伪装成数据服务的无效结果，并产生 8 测试 USDC 支付请求。
6. 该请求没有超过金额和时间等硬约束，因此通过普通规则检查并在测试网结算；演示明确标记这是合成的受控攻击。
7. 系统或用户创建 Claim。
8. 系统验证 Claim Packet，显示授权与实际交易的差异。
9. AI 输出事故类型、条款匹配、证据引用、置信度和缺失项。
10. 人工演示审核员批准或拒绝。
11. ClaimVault 执行测试资产赔付，并显示交易链接。
12. 用户切换到“用户明确授权”对照案例，系统拒赔并说明除外责任。
13. 系统生成最终案件包版本，完成摘要存证后允许导出；页面列出本次披露内容和缺失项。
14. 评委在独立 CLI 中使用自己的信任配置验证原包，再验证修改过的副本，观察对应检查失败。

### 16.1 合成场景的客观判定契约

为避免“AI 主观宣布服务无效”，演示夹具预先定义、签名并版本化 `ValidRiskReport` 契约：报告主题必须匹配 Mandate，包含指定章节、至少两个可定位来源、生成时间、内容哈希和交付回执；候选供应商身份与其声明能力也写入场景 Manifest。

攻击样本在金额、时间和候选商户等硬字段上全部合规，返回结果的形式也看似完整，但正文被外部隐藏指令劫持，核心内容与所购风险主题不一致。场景作者在不提供给 AI 的 ground-truth 文件中标注预期类别及理由；评测器用它评分。模型必须引用实际输入片段得出语义偏离，不能读取答案标签。

## 17. Epics and User Stories

### Epic 1：创建清晰、可验证的支付授权

#### Story 1.1

作为 Agent 操作者，我希望在任务执行前看到并确认支付边界，以便知道 Agent 能做什么、不能做什么。

验收标准：

- 页面展示任务、金额上限、币种、允许收款人、有效期和确认模式；
- 用户未确认时，Agent 不能开始受保护支付；
- 确认后生成唯一 Mandate ID；
- 用户可以查看用于判断理赔的保障条款和除外责任；
- 授权内容在任务开始后不可被无痕修改。

#### Story 1.2

作为独立验证者，我希望确认 Mandate 确实先于交易存在，以便排除事故后补写授权。

验收标准：

- 系统展示 Mandate 时间、签名状态和内容摘要；
- 修改 Mandate 后，验证状态变为失败；
- Mandate 与 Payment ID、Agent Run ID 可以互相跳转。

### Epic 2：记录与支付责任有关的Agent执行证据

#### Story 2.1

作为 Agent 操作者，我希望看到关键执行时间线，以便理解支付是如何产生的。

验收标准：

- 时间线至少包含任务开始、外部输入、关键工具调用、支付请求、钱包执行和交易结算；
- 每个事件显示来源、时间、类型和验证状态；
- 敏感原文默认不公开，仅显示必要摘要或哈希；
- 页面不得声称展示完整 chain-of-thought。

#### Story 2.2

作为风险负责人，我希望所有受保护支付都经过统一入口，以便证据不会在付款完成后才补录。

验收标准：

- 受保护支付显示 Gateway 记录；
- 绕过 Gateway 的交易显示 Unprotected；
- Unprotected 交易不能被系统标记为证据完整；
- 重复提交相同支付请求时，系统在时间线上明确显示重试与结果。

### Epic 3：生成并验证Claim Packet

#### Story 3.1

作为受损用户，我希望从一笔已结算交易创建 Claim，以便启动调查。

验收标准：

- 用户能从交易详情创建 Claim；
- 未结算、已回滚或不存在损失的交易不能直接进入赔付；
- 重复 Claim 会指向已有案件，而不是产生两个独立赔付机会；
- 创建后立即显示证据收集进度。

#### Story 3.2

作为获授权的验证者，我希望使用登记或公开的信任材料检查证据完整性，以便不依赖平台口头保证。

验收标准：

- 系统分别显示签名、执行环境证明与 P0 最小链上存证结果；P0 必须项尚未实现时显示 `Not Implemented`，该项 P0 不得验收通过；硬件 TEE attestation 未实现时显示 `Not Present`。P1 连续哈希链与 Merkle proof 未实现时显示 `Planned / Not Present`；
- 任一证据被修改后，对应验证项失败；
- 验证失败不会被 AI 建议覆盖；
- 用户可以查看付款交易、付款前授权存证和指定包版本的存证引用；缺少关键引用时不得显示完整验证通过；
- 验证页面列出 Evidence Manifest、签名者 `key_id`、信任根、验证程序哈希（软件级为构建哈希，硬件 TEE 时为 measurement）、验证时间与无法验证项；
- 公众只能验证已披露的哈希和链上引用；受限原文仅向有权限的审核主体开放；
- 页面明确提示：完整性通过不等于来源真实或因果关系已证明。

#### Story 3.3

作为获授权用户，我希望导出一份版本固定的案件材料，以便交给另一位审核员，并在平台不可用时保留核验能力。

验收标准：

- ZIP 内容、版本、文件摘要、披露范围与 Manifest 一致；包内不得出现密钥或未获授权原文；
- 更换平台展示页面不影响验证器对同一包的结果；
- 补证、人工决定、赔付回执更新生成新版本与新承诺，旧包仍可验证；
- 受限版与公开版分别标明可验证范围，不将公开版的部分通过冒充完整案件通过。

#### Story 3.4

作为评委，我希望在平台外验证证据，并发现被改动的副本，以便判断产品是否提供真实独立核验能力。

验收标准：

- 独立 CLI 的信任配置随其开源仓库版本化发布，不调用平台 API 或模型，并拒绝包内信任材料；
- 验证原包后，篡改关键文件、删文件、替换签名者、伪造链引用均得到准确失败或未知结果；以旧版本冒充最新的样本得到 Superseded 提示；
- 离线模式与 RPC 故障模式明确列出不能验证的链上检查，不显示全部通过；
- 即使攻击者重算文件摘要并用自有密钥签名，也不能通过原可信签名者与原链上承诺检查；
- 执行环境证明的签名校验失败显示 Fail；硬件 TEE attestation 项在 P0 显示 Not Present，任何情况下不因此显示全部通过；
- 输出机器可读结果和人可读摘要，不把完整性通过解释为赔付结论成立，且不得超出 §4.3 的验证结论上限。

#### Story 3.5

作为审核员，我希望核对付款前授权和付款后证据包的链上承诺，以便确认事后材料未被悄悄替换，并区分不同时间点能支持的结论。

验收标准：

- 授权存证交易区块高度 + N（可配置，默认 1）≤ 支付交易区块高度时才发起受保护付款；同一区块视为不满足；存证 Failed 或超时（默认 60 秒）时支付路径终止并标记 Unprotected；
- Manifest 包承诺与链上记录按固定算法匹配；相同承诺重复写入幂等成功，不同承诺写入同一（提交者，引用标识，版本）被拒绝，重复提交不覆盖同版本记录；
- 上链内容不包含敏感原文，原文变更生成新包和新存证；
- 待确认、失败、错误链、错误合约、错误提交者或未知信任根不显示通过；
- 事后包存证不得被描述为事故发生前全过程已经真实存在的证明。

### Epic 4：AI辅助事故归因与条款判断

#### Story 4.1

作为案件审核员，我希望快速看到事故时间线与授权偏差，以便减少人工翻查多套日志的时间。

验收标准：

- 报告列出用户授权与实际支付的逐项比较；
- AI 输出符合固定 schema，并把 `primary_hypothesis` 明确标为假设；
- 每项关键判断链接到真实存在的 Evidence ID，不得生成不存在的引用；
- 报告显示候选原因、支持与反对证据、置信区间、矛盾、缺失证据和模型局限性；
- AI 不得直接触发真实赔付。

#### Story 4.2

作为案件审核员，我希望看到条款匹配结果，以便判断该事故是否可能符合演示保障规则。

验收标准：

- 报告引用具体保障条款或除外责任；
- 金额超限、收款人不符等硬事实由规则结果呈现；
- 证据不完整时结果为 Needs Information，而不是强行批准或拒绝；
- 对“用户明确批准最终交易”的案例，系统引用除外责任并建议拒赔。

#### Story 4.3

作为评委，我希望 AI 能力通过固定测试集被单独验证，以便确认它不是对演示答案的硬编码包装。

验收标准：

- 仓库包含至少 6 个固定案件：Prompt Injection、用户明确授权、重复支付、证据缺失各至少 1 个；
- 所有关键 AI 结论引用的 Evidence ID 和 Policy Clause ID 必须 100% 存在；
- 缺少决定性语义证据时，模型必须输出补证或低置信度，不得自动批准；
- 至少有 1 个无攻击的相似对照样本，用于检查误报；
- 确定性硬字段由规则引擎计算，AI 不能改写；
- 演示记录模型版本、提示模板版本与每个固定案件的实际输出，失败结果也如实保留。

通过门槛：硬字段改写次数为 0；所有缺证夹具进入 `Needs Information`；无攻击对照不得输出高置信 Prompt Injection；若任一硬门槛失败，AI 能力不得在提交材料中标为通过。

### Epic 5：人工决定与测试赔付

#### Story 5.1

作为演示审核员，我希望在完整证据和 AI 建议之上作出决定，以便保留明确责任主体。

验收标准：

- 批准、部分批准、拒赔和补证均要求填写理由；
- 决定记录审核员、时间和所依据的证据；
- 未经人工确认不得执行赔付；
- AI 建议与人工决定不一致时，两者都被保留。

#### Story 5.2

作为受损用户，我希望看到赔付结果和可验证回执，以便确认资金恢复已经完成。

验收标准：

- 批准后显示赔付金额、测试资产和交易状态；
- 支付成功后提供测试网交易链接；
- 交易失败时 Claim 保持 Approved，而不是错误显示 Paid；
- 赔付请求的幂等键为 `claim_id + decision_version`，同一决定不能重复执行；
- 交易在设定的测试网最终性确认数达到后才从 `Payout Pending` 变为 `Paid`；
- 交易失败进入 `Payout Failed`，重试沿用同一幂等键并保留每次广播记录；
- 商户退款或追回资金发生在决定前时，重新计算 `eligible_loss`；发生在测试赔付后时，标记为待人工追偿，不静默改写历史决定。

### Epic 6：让评委独立理解系统边界

#### Story 6.1

作为评委，我希望在一个界面看懂哪些部分真实运行、哪些是模拟和人工步骤，以便判断项目可信度。

验收标准：

- 页面标明测试网、合成攻击、外部模型、执行环境证明方式（软件级或硬件 TEE）和人工确认；
- 提供一键验证证据入口；
- 提供正常、获赔和拒赔三个可复现结果；
- 不展示或使用真实客户金融数据、主网高价值资产或私钥；
- 所有失败降级路径都有明确标签，不以预录冒充实时运行。

## 18. V1事故分类

| 类别 | V1示例 | 硬证据 | AI作用 | 默认处理 |
|---|---|---|---|---|
| Prompt Injection | 恶意页面诱导 Agent 在预算内购买与任务不符的伪装服务 | 输入来源、任务语义、工具调用与结果不一致 | 识别恶意语义与因果链 | 可承保候选 |
| Agent Policy Deviation | Agent 自行突破金额上限 | Mandate 与交易比较 | 解释可能原因 | 可承保候选 |
| Duplicate Execution | 超时重试导致重复付款 | 相同意图对应多笔结算 | 区分正常分批与异常重试 | 可承保候选 |
| User-Authorized Action | 用户明确确认最终错误交易 | 用户签名与最终参数一致 | 摘要说明 | 默认拒赔 |
| Evidence Failure | 证据缺失、签名或证明无效 | 验证失败 | 列出缺口 | 补证或拒绝处理 |

## 19. Edge Cases

### 授权与执行

- Mandate 在交易广播前过期；
- 用户修改授权但 Agent 使用旧版本；
- 一个任务产生多笔合法付款；
- 收款地址改变但商户身份未变；
- Agent 请求金额合规，但累计金额超预算；
- 同一工具调用因网络超时重复执行。

### 证据

- Langfuse 或观测日志存在，但可信证据链缺失；
- TEE attestation 过期、无法验证或程序哈希不匹配；
- 部分原始数据因隐私策略不可披露；
- 链上锚定成功但交易回执暂未最终确认；
- Agent Run ID 与 Payment ID 无法绑定；
- 外部内容已删除，只剩事故前生成的哈希和摘要。

### 理赔

- AI 置信度低；
- 规则与 AI 结论冲突；
- 人工否决 AI 的批准建议；
- 用户提交重复 Claim；
- 赔付合约交易失败；
- 用户在清晰确认最终交易后反悔；
- Claim 金额超过演示保障上限；
- 事故同时包含提示词攻击与配置失误。

对应原则：证据不完整不等于自动拒赔，但不得自动批准；验证失败不得被语言模型覆盖；没有实际损失不得产生赔付；所有灰色案件回到人工。

### V1隐私与安全控制

黑客松版本只使用合成任务、合成攻击和测试资产，不导入真实客户金融数据。V1 最低控制包括：

- 原始外部内容、用户指令与模型输入按 `Public / Restricted / TEE-only` 分类；
- UI 默认展示摘要和哈希，受限原文需要案件权限才能查看；
- 传输使用 TLS，存储加密，租户与案件按角色隔离；
- 对 Evidence Packet 的查看、导出、AI 调用和人工决定记录访问审计；
- 第三方模型调用仅发送必要的脱敏片段，并记录供应商、区域和用途；生产接入必须通过合同或服务配置禁止将案件数据用于模型训练；
- 演示数据设定明确保留期并支持删除；法律保全、真实客户删除请求和跨境传输属于生产 Gate；
- 密钥不写入日志或 Claim Packet；信任配置携带签名密钥注册表：每个 key_id 记录公钥、valid_from、valid_to，验证历史包时按证据 observed_at 选择当时有效密钥；轮换后旧密钥保留在注册表中并标记 retired，注册表未覆盖时间段内的签名输出 Unknown；
- 任何无法满足数据区域、跨境、保留或外部模型约束的客户，默认关闭相应 AI 外发路径并转人工。

## 20. What We Are Building：黑客松范围

### P0：必须现场跑通的一条主干

P0 只保留一条单页控制台可连续演示的受控路径：

1. 用户签署本项目格式的 Mandate（EIP-712，domain.chainId=1439），启动一个受控 Agent 购买任务；
2. 付款前完成授权承诺存证并满足 §4.4 的区块高度规则；付款请求经过 Evidence Gateway（只做约束检查与证据签名，不托管用户资金），在 Injective EVM 测试网结算；评审现场可以使用预先结算但可复验的攻击交易，避免等待链确认；历史预置交易同样接受“授权先于付款”检查，不满足时对应检查显示 Fail 并标注为历史遗留，不得隐藏；
3. Gateway 生成签名 Evidence Manifest，并把 Mandate、关键语义输入、工具调用和交易回执绑定为一个 Claim Packet；
4. 确定性证据验证由独立验证者服务执行：以单独密钥对（验证程序哈希 + 输入摘要 + 输出摘要）签名形成软件级执行环境证明，并封装 AI 所见输入与输出摘要；页面与包内如实标注其信任根为验证者密钥而非硬件；
5. AI 对“硬约束通过但语义目标被劫持”的合成 Prompt Injection 案例输出结构化原因假设、条款映射和 Evidence ID 引用；
6. 演示审核员在同一控制台作出决定，ClaimVault 以合约级幂等（claim_id + decision_version）执行测试资产恢复；
7. 最终包（含决定与赔付回执）承诺存证并导出版本化 ZIP；P0 只锚两点：授权承诺与最终包承诺，中间版本由 Manifest 签名与前版承诺链绑定；
8. 开源独立 CLI：离线检查（摘要、签名、对象关联）与在线检查（两处锚定、支付与赔付回执、版本新鲜度）；现场分别核验原包与篡改包；硬件 TEE attestation 项显示 Not Present；
9. 页面同时标明测试资产、合成攻击、合成保障条款、外部模型、执行环境证明方式（软件级）、人工步骤和未实现能力。

用户明确授权后的拒绝案例作为签名固定夹具随仓库提供，P0 只需现场加载并得到正确结果，不要求再运行第二次完整 Agent 支付。

P0 验证集合为：Mandate 签名与付款前授权存证、Evidence Manifest 签名与对象关联、软件级执行环境证明、测试网交易回执、最终包承诺存证、导出包与独立 CLI 核验（含 Superseded 检查与篡改负例集）。硬件 TEE attestation、完整连续哈希链、批量 Merkle proof 与逐版本存证均属于 P1，未实现时显示 `Planned / Not Present`。

评审现场必须实时运行 Claim 验证、AI 分析、人工决定、一次测试资产恢复和独立 CLI 验证。完整路径须保留真实连续运行记录。若现场使用已确认的授权、攻击付款或包存证，必须标注其运行时间和版本，独立验证展示该历史包的实际状态，不能冒充本次审核的新包。最终包在新决定或新回执加入后须重新存证，未确认前明确显示 Pending。预置交易与环境故障降级按赛制披露，需批准的降级由主办方确认。

### P1：有时间再增强

- 为所有事件构建连续哈希链、批量 Merkle root 锚定与逐版本存证，降低逐包存证成本；P0 已包含授权承诺与最终包承诺两点存证，不要求 Merkle 树；
- 真实 TEE/CDE 保护点（如 Phala Cloud、Marlin Oyster 或机密 VM），将执行环境证明从软件级升级为硬件 attestation，包格式与验证器接口不变；CLI 对 TEE attestation 的 quote 级密码学验证（含信任根与 collateral 获取）随之启用；
- Bank/MTS precompile 的原生 denom 测试赔付演示（MultiVM 互操作叙事；本地 fork 测试需 InjectiveLabs 补丁版 Foundry）；
- 现场完整运行第二个拒绝案例；
- Langfuse / OpenTelemetry 可视化辅助证据；
- 多模型比较、申诉、多级审核和复杂部分赔付；
- AP2、Mastercard VI、x402 或 t54 的真实适配器。

### 可以降级但必须诚实标记

- 不假设主办方或运行环境提供 CDE；P0 一律使用软件级执行环境证明，如临时获得真实 TEE 条件，仅作为增强展示并如实标注；
- 外部模型失败时，保留证据验证和确定性规则结果，将 AI 状态标为不可用；
- 测试网拥堵时，使用已广播交易或经主办方允许的替代测试网，并保留复现记录；
- 演示中若攻击结果预先构造，必须标为合成攻击，不得称为对真实系统攻击。

### 技术基线（已定方向）

- 目标链：Injective EVM 测试网，EVM Chain ID 1439（对应 Cosmos chain-id injective-888，仅作网络标识，不参与任何 EVM 签名参数）。主网 EVM Chain ID 1776 保持 NOT DEPLOYED：部署脚本默认测试网，主网需显式 flag 且在 P0 阶段禁用。不使用已被官方废弃的 inEVM。
- 合约：Solidity + Foundry；EvidenceAnchor 与 ClaimVault 为普通最小合约，不使用 precompile；Bank/MTS precompile 为 P1 可选；Exchange precompile 不属于核心场景。
- 前端与独立 CLI：TypeScript + viem；viem 无内置 Injective 链，须 defineChain({ id: 1439 })；EIP-712 的 domain.chainId 必须使用数字 1439，严禁误用字符串 injective-888。
- 交易与 gas：按 Injective 官方教程显式指定 gas price 与 gas limit；EIP-1559 支持在官方页面间口径不一致，P0 默认 legacy（type-0）交易，type-2 仅在实际验证支持后启用。
- RPC：默认公共端点（测试网 https://k8s.testnet.json-rpc.injective.network/）；官方明确公共端点重度限流且截断历史数据查询，信任配置与平台配置必须支持替换为归档或第三方 RPC；确认数一律用显式区块高度差计算，不使用 finalized/safe block tag；核对承诺优先用 eth_call 读合约存储，回执查询失败输出 Unknown。
- 浏览器：测试网 Blockscout（https://testnet.blockscout.injective.network/）仅作展示，链接不代替 RPC 验证。
- 资产：测试 INJ（官方水龙头支持 0x 地址直接领取）或明确标记的测试代币；不声称真实 USDC 或真实保险赔付。
- 部署形态：演示前端、控制台、Gateway 与验证者服务全部自托管（自有机器部署），不依赖主办方提供 CDE、托管环境或内网服务。
- 文档基准：Injective 官方页面间存在数据漂移（RPC URL、代币地址均有不一致版本），一律以 developers-evm/network-information 页为准，部署配置随仓库发布。

### 解封条件（进入 Technical Spec 前必须完成的 Spike）

1. MetaMask 在 chain 1439 完成 EIP-712（eth_signTypedData）签名，链下 ecrecover 验证通过——Injective 官方文档未说明 EVM 侧 typed-data 支持（其 EIP-712 材料均针对 Cosmos 侧），此 Spike 通过前 §5.1 授权存证保持“待技术验证”；
2. Foundry 部署最小 EvidenceAnchor 到测试网，viem 经可配置 RPC 读回承诺、事件与回执，并确认 legacy 交易与显式 gas 参数行为；
3. CLI 原型对一个手工构造的包跑通离线与在线检查，四态输出正确，篡改负例集（改文件、删文件、替换签名者、伪造链 ID、旧版本冒充最新）全部得到 Fail / Unknown / Superseded。

三个 Spike 全部通过前，本文状态保持 UPDATED DRAFT / 待技术验证，所有能力维持 NOT STARTED / NOT VERIFIED。

## 21. What We Would Add With More Time

- Mastercard VI、AP2、x402 等多协议适配；
- 面向 Agent 框架的 Recorder SDK；
- 多 TEE 或多证明信任根；
- 保险公司与 MGA 的真实案件系统接口；
- 支持企业自保、保证金和现有退款轨道；
- 欺诈检测、申诉与多级审核；
- 事故聚合、风险定价和保障额度建议；
- 跨 Agent、模型、工具和商户的匿名风险基准；
- 面向监管或审计机构的证据导出；
- 非支付高风险 Agent 行为，但只在支付楔子验证后扩展。

## 22. Success Metrics

### 黑客松成功标准

- 核心路径可以在测试网与自托管环境连续运行；
- Prompt Injection 主路径可重复运行，拒绝案例固定夹具可重复验证；
- Claim Packet 的签名、Manifest 与执行环境证明可以由评委触发验证；
- P0 导出包可脱离平台后端验证，授权与包承诺可经独立 RPC 核验；篡改、缺失、不可信签名者、伪造链引用与旧版本冒充最新均有负例记录；CLI 对无法完成验证的项输出 Unknown 或 Not Present 而非通过；
- 若实现 P1，连续哈希链和 Merkle proof 也可单独触发验证；
- AI 报告符合固定 schema，每个主要结论引用有效 Evidence ID；
- 至少 6 个固定 AI 案件完成评测，缺证案件会补证或降低置信度，且包含无攻击对照；
- 人工批准前不能执行赔付；
- 修改任一关键证据会导致验证失败；
- 同一 `claim_id + decision_version` 无法产生重复测试赔付；
- 3 分钟视频包含真实运行，不依赖纯 PPT；
- 团队能清楚回答“去掉 AI”“去掉链”“去掉 TEE”分别损失什么能力。

### 早期创业验证标准

- 至少 2 家 Agent 支付/钱包团队愿意评审 Claim Packet；
- 至少 2 名承保或理赔从业者说明哪些字段可以或不能用于真实判断；
- 至少 1 个设计伙伴愿意提供测试工作流或接入意向；
- 能从真实或合成的受保护交易中稳定重建事故时间线；
- 证明证据/理赔 API 能减少人工调查步骤，而不仅是生成更漂亮的报告。

## 23. Submission Proof Points

### 对应“技术实现与创新性”

- Agent 支付、执行环境证明（软件级）、链上存证和 AI 理赔形成一个运行闭环；
- AI 不只是聊天框，承担非结构化事故归因、证据缺口识别和条款映射。

### 对应“问题定义与场景价值”

- 解决的是 Agent 支付发生后无法归责与恢复的现实问题；
- 首个买方是 Agent Wallet / 支付网关的风险与争议团队；保险方先作为设计和承保伙伴，而不是假设所有散户逐笔买保险。

### 对应“隐私、安全与可验证性”

- 原文最少披露；
- 关键事件有哈希、签名和软件级执行环境证明；版本化证据包可导出，授权和包承诺在测试网上存证；
- 评委使用独立 CLI 和包外信任配置核验，篡改副本触发失败；
- 公众可验证已披露哈希和链上引用，获授权主体可使用登记的信任材料验证受限证据；
- 明确披露信任根和无法证明的内容。

### 对应“合规意识”

- 只使用测试资产和合成事故；
- V1 只执行合成保障规则，不销售保险、不托管真实赔付资金、不提供最终法律责任判定；
- 演示由人工掌握最终决定；生产中的保单、客户沟通、保障决定和赔付由获许可主体拥有；
- 任何真实保单、司法辖区和按理赔金额收费模式先经过法律与合规 Gate；
- 外部模型和跨境数据使用明确披露。

## 24. 3分钟演示脚本

三分钟提交视频包含同一条路径的连续真实运行，录制前测量模型响应、授权存证、支付、包存证与赔付确认耗时。不得通过隐藏剪辑或伪造成功状态满足时长；若超过预算，调整测试环境或简化非关键交互。现场展示窗口按 AIX 文档为 5—8 分钟，可容纳完整解释与核验。

| 时间 | 演示动作 | 评委应看到的证据 |
|---|---|---|
| 0:00–0:15 | 说明 AI 在预算内也可能买错东西 | 明确的金融事故与目标用户 |
| 0:15–0:35 | 签署授权并完成授权存证 | 付款前授权承诺、确认状态 |
| 0:35–1:00 | 受控攻击与测试付款 | 合成恶意输入、真实测试交易 |
| 1:00–1:25 | 核验证据并运行 AI 分析 | 引用实际材料的语义偏离假设、执行环境证明方式 |
| 1:25–1:50 | 人工批准并完成测试赔付 | 人工理由、测试回执、净损失金额 |
| 1:50–2:10 | 最终包存证、导出 | 包版本、链上承诺、披露范围 |
| 2:10–2:40 | 独立 CLI 核验原包及篡改副本 | 原包逐项通过；篡改项失败；不依赖平台后端 |
| 2:40–2:55 | 加载明确标注的拒赔对照夹具 | 用户明确授权对应的除外条款 |
| 2:55–3:00 | 说明产品边界 | 中心化审核、独立可验证证据、测试赔付 |

上述时间是待实测的目标，不是性能承诺。网络等待必须可见。独立验证原包时若关键检查 Unknown，应现场解释原因，不能用绿色结果替代。展示已确认历史包时应明确标为历史验证样例，不与本次生成的最终包混用。

建议路演表述：

> 我们给能自主付款的 AI 做事故记录和处理系统。用户可以带走证据包，第三方不用登录我们的平台也能检查材料有没有被改动，并核对链上的存证。AI 帮忙调查，人来决定是否按演示规则赔付。本次使用合成事故和测试资产；审核仍由明确主体负责。

## 25. 风险与待验证假设

### 高风险假设

- Agent 支付平台愿意强制交易经过第三方 Gateway；
- 保险方会接受此类证据作为调查输入；
- 事故频率和损失规模足以支持 B2B 产品，而不是只靠现有限额与退款；
- 中立跨协议层不会快速被单一支付网络吸收；
- 关键上下文可以在不收集完整敏感内容的情况下支持归因。

### 商业上必须尽快回答

- Agent Wallet / 支付网关是否真的愿意成为第一位付费买方，而不仅是设计伙伴？
- 买方按交易量、受保护额度还是 Claim 付费？
- 现有 cyber/E&O、chargeback 和退款流程具体在哪些案例拒绝承担损失？
- 保险方决定赔付时真正需要哪十个字段？
- 对高频低额 x402 交易，应采用月度聚合保障还是只保护高价值交易？

## 26. The Assignment

在继续扩大功能前，制作一页可展示的 Claim Packet 样例，分别交给：

- 两名保险承保或理赔从业者；
- 两家 Agent 钱包、x402 或支付平台团队。

只问三个问题：

1. 如果客户声称 Agent 转错了钱，这些证据够不够你决定下一步？
2. 哪个字段没有价值，哪个关键字段缺失？
3. 如果它能把事故调查从数小时压缩到数分钟，谁拥有预算购买？

把回答原话记录下来。至少一家 Wallet / 支付网关愿意继续提供测试工作流或讨论接入，且至少一名保险从业者认可其能进入案件调查，才把“保险理赔基础设施”视为获得了第一层创业验证。

---

## 27. 当前结论

Agent Flight Recorder 的黑客松版本不是一家保险公司，而是一个可运行的“授权—事故—证据—AI 理赔—人工批准—测试赔付”闭环。

它的创业版本也不是逐笔微型保险，而是为 Agent 支付平台和保险机构提供协议无关的事故证据与恢复基础设施。Mastercard VI、AP2、x402 与现有观测工具不是需要重造的竞争底座，而是 Claim Packet 的上游证据来源。

本项目最需要保护的产品纪律是：

> 不承诺阻止一切事故，不承诺 AI 自动定责，不承诺无牌照赔付；只承诺将一次原本说不清的 Agent 支付事故，转换成可验证、可审核、可执行恢复的案件。

## 28. v1.2 对应 AIX 的交付检查

| AIX 要求 | v1.2 对应交付 | 验收边界 |
|---|---|---|
| 可运行核心路径 | 授权、攻击、支付、验证、分析、审核、测试赔付和证据导出 | 代码、复现步骤、真实运行与失败记录 |
| AI 实质参与 | 分析硬字段正常但任务语义偏离的事故 | 至少 6 个固定案件；引用与缺证处理通过原定门槛 |
| C 方向关键步骤可验证 | 确定性验证与 AI 输入输出摘要的独立签名绑定、链上承诺；硬件 TEE 为 P1 | 不声称证明外部模型内部推理或结论正确；不声称软件级证明等同硬件 attestation |
| 独立可复核报告 | 版本化 ZIP 与开源 CLI | 平台后端断开后可验证已持有包；链检查仍需 RPC |
| 隐私和边界 | 最小披露、受控导出、带盐承诺、测试资产 | 公开版部分可验不等于完整案卷可验 |
| 演示真实性 | 原包通过、篡改失败、拒赔对照、明确人工节点 | 披露历史包、预录、网络及外部模型依赖 |

提交包补充验证器源码、独立验证使用说明、固定算法测试向量、随 CLI 仓库发布的信任配置及其取得方式、原包与篡改负例（含 Superseded 样本）、EvidenceAnchor 合约地址和部署配置，以及 §20 三个解封 Spike 的结果记录。记录包体积、导出耗时、离线和在线验证耗时、存证确认耗时及费用；不虚构性能数字。

AIX 所附执行版列出的评分权重合计为 95%，与总分 100 分不一致，须向主办方核对（责任人：提交负责人；时限：正式提交前）；不得自行补齐权重。本次修订不改变所有能力尚未实现、尚未验证的状态。
