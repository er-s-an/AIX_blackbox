# 源码上传前验证 · 2026-09-06

在独立的公开源码工作目录安装锁定依赖，未复制原实例的密钥、数据库或运行缓存。

| 检查 | 当前结果 |
| --- | --- |
| `pnpm install --frozen-lockfile` | 通过 |
| `pnpm test` | 35 项单元测试、10 项合约测试通过；合约 fuzz 512 次 |
| `pnpm build` | TypeScript 与 Next.js production build 通过 |
| `pnpm test:cold` | 无 Git 的新目录安装成功；生成独立密钥；新 npm 项目安装实际 SDK tgz 并检查类型；采集、导出与 CLI 核验通过 |
| 冷安装 HTTP 边界 | 401 / 400 / 413 / 429 符合预期；recorder-only 拒绝受控付款流程 |
| 冷安装快照范围 | 签名者信任通过；整体为 `incomplete`，未误报完整最终包 |
| `pnpm test:ablation` | 去重、先持久化请求、签名失败拒绝三项本地对照符合预期 |
| 外部 Agent 示例 | esbuild 编译通过；此次未重跑交互暂停或模型与测试网付款 |

消融中，移除去重会让同一操作调用付款函数两次；移除请求持久化会让付款开始时磁盘缺少请求；忽略签名失败会错误接受被篡改并重算清单的材料。对照代码只在临时目录构建，不进入服务或 SDK 的运行开关。

本次验证没有调用模型、充值钱包、部署合约或执行付款。历史真实交易和模型运行仍以 [Beta 记录](../product/BETA-RESULTS.md) 的日期、输入与范围为准。公开的是源码；此检查不代表 npm 发布、托管服务上线或完整金融产品验收。
