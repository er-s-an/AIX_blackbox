# 历史验证资料

本目录保存开发阶段的验证输出。时间、测试环境与源码版本以各文件记录为准；这些历史结果不能替代新版本实测。

用于公开源码的副本已将个人机器路径替换为 `<workspace>` / `<home>`。原始私有运行目录与密钥不随仓库发布，交易哈希、签名和结果字段未因路径脱敏而修改。

从新安装复现本地检查，请使用根 README 的 `pnpm test`、`pnpm build`、`pnpm test:cold` 和 `pnpm test:ablation`。需要历史案件 ZIP 的回归命令须额外取得对应归档及公开信任配置。
