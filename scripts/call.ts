import {readFileSync,writeFileSync} from 'node:fs';import {parse} from 'dotenv';const env=parse(readFileSync('.secrets/gateway.env'));const login=await fetch('http://127.0.0.1:4311/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({role:'operator',password:env.OPERATOR_PASSWORD})});const cookie=login.headers.get('set-cookie')!.split(';')[0]!;const path=process.argv[2];if(!path)throw new Error('PATH_REQUIRED');const r=await fetch('http://127.0.0.1:4311'+path,{method:'POST',headers:{'Content-Type':'application/json',cookie},body:process.argv[3]||'{}',signal:AbortSignal.timeout(240000)});const data:any=await r.json();writeFileSync('runs/LAST-CALL.json',JSON.stringify(data,null,2));console.log({http:r.status,id:data.id,status:data.status,claim_status:data.claim_status,error:data.error,public:data.public_versions?.at(-1)?.verification.overall});process.exitCode=r.ok?0:1;
