import {readFileSync,readdirSync} from 'node:fs';import {parse} from 'dotenv';import {sealedFiles} from '../packages/core/src/sealed-file.ts';const env=parse(readFileSync('.secrets/operator.env')),journal=sealedFiles(env.OPERATOR_PK!);let migrated=0;for(const file of readdirSync('.runtime/operator').filter(n=>n.endsWith('.json'))){const path='.runtime/operator/'+file;const raw=JSON.parse(readFileSync(path,'utf8'));if(raw.format!=='afr-sealed/1'){journal.write(path,raw);if(JSON.stringify(journal.read(path))!==JSON.stringify(raw))throw new Error('JOURNAL_MIGRATION_MISMATCH');migrated++;}}console.log({migrated,verified:true});
