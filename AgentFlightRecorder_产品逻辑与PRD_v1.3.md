# Agent Flight Recorder：产品逻辑与 PRD v1.3

> 定位：面向 Agent 支付平台与保险机构的可验证事故证据、理赔工作流与恢复连接基础设施（本项目不持有、不支付真实资金），支持证据包导出、脱离平台独立验证与证据摘要链上存证。
>
> 一句话：Payments let agents spend. We make their mistakes recoverable.

**文档日期：** 2026-09-05\
**状态：** UPDATED DRAFT｜第二轮 Review 修订版；§20 四个解封 Spike 全部通过前不得进入 Technical Spec\
**阶段：** 黑客松原型，同时保留创业落地路径\
**实现状态：** NOT STARTED / NOT VERIFIED；本文描述目标与验收要求，不代表代码已经完成\
**主赛道建议：** C｜可验证的 AI 推理审计，关联 D｜AI × Web3 / Fintech；B 为辅助关联方向。

**v1.2 变更：** 依据只读 Review 修订：①包承诺改为两阶段构造，闭合 manifest 文件清单与 anchors.json 之间的循环依赖；②"授权先于付款"改为显式区块高度规则（默认确认数 N=1，目标链为即时最终性），并补充存证失败/超时与支付路径的耦合；③独立验证器信任配置随其开源仓库版本化发布，拒绝包内信任材料；④执行环境证明的 CLI 验证收缩为签名与程序哈希白名单匹配，并新增验证结论上限；⑤P0 存证收缩为授权承诺与最终包承诺两点，逐版本存证与 quote 级 attestation 验证降为 P1；⑥统一验证四态（Pass / Fail / Unknown / Not Present）与 Manifest 状态映射；⑦补充锚定幂等、密钥轮换注册表、RFC 8785（JCS）规范化、Superseded 过期版本检测；⑧计价措辞统一为明确标记的测试资产；⑨新增技术基线（Injective EVM 测试网 1439、legacy 交易与显式 gas、RPC 可配置与归档选项、主网 1776 保持 NOT DEPLOYED）与三个解封 Spike；⑩执行环境证明改为软件级签名（不假设官方 CDE，前后端自托管部署），硬件 TEE 降为 P1，包格式与验证器接口不变。原 v1、v1.1 保留供比较，所有能力仍为 NOT STARTED / NOT VERIFIED。

**v1.3 变更：** 依据第二轮只读 Review 修订，优先修正 v1.2 修复自身引入的问题：①两阶段构造收窄——链上定位键（类型、链 ID、合约、提交者、引用标识、版本）与授权存证完整引用进入 Manifest 的 `anchors_expected` 并纳入承诺，anchors.json 只保留包存证 tx hash/区块号提示；文件清单排除集合固定为三项；验证器按定位键 eth_call 比对承诺，不依赖 anchors.json；②锚定键改为 (submitter, type=AUTH|PACKET, ref_id, version)，ref_id 按谱系与披露集合派生，解决多披露集合与授权/包锚共享标识的冲突；合约记录写入区块号；③"授权先于付款"拆为顺序间隔 G 与确认深度 D 两个参数，新增生效授权版本规则，新增 Payment `Aborted` 终态与 Mandate 复用规则；④验证结论上限补充"执行环境证明（软件级）= 验证者签署声明，不证明程序实际执行"，信任配置内容为运营方自登记的语义写入 CLI/UI 标签；Superseded 明确为包级 advisory 标记；⑤Story 4.3 新增正向门槛（PI 夹具必须被识别并引用 must-cite 证据）、每夹具 ≥3 次运行、扰动集、确定性评分器、ground-truth 隔离与输入摘要挂钩、`quotes` 字段；§16.1 契约条款逐条标注规则/AI 归属；⑥Story 5.2 赔付失败态统一为 `Payout Failed`；Claim 新增 `Not Eligible`、`Rejected for Processing` 终态与 `fraud_suspected / escalated / recovery_pending` 标记；Partially Protected 收窄为 Mandate 级汇总；⑦演示：时间轴按 LLM 段重新预算，声明"带屏显标注的可见时间压缩允许、剪切与状态伪造禁止"，新增 Spike 4 端到端计时；预置交易理由改为外部模型延迟并要求满足全部规则；每会话独立预置支付；CLI 默认归档 RPC；⑧商业与合规：创业部署信任模型、理赔处理牌照风险、Mandate UX 摩擦进入 §25；"恢复"措辞加限定；VI 竞争定位重写；⑨§10.1 "数据模型兼容"列改为"与外部标准的关系"，x402 声称降为字段命名参考并要求映射表；演示资产改名 `AFR-TEST-USD`；`AFR Mandate` 前缀；⑩术语与状态词统一（`Not Implemented / Planned` 为 UI 能力徽章、`TEE-only` 在 P0 的处理、§19/§22 TEE 措辞、scenario.json 命名）。原 v1、v1.1、v1.2 保留供比较，所有能力仍为 NOT STARTED / NOT VERIFIED。

---

# 第一部分：项目完整逻辑

## 1. 我们观察到的变化

AI Agent 正从“给建议的软件”变成“可以产生经济后果的软件”。它开始自主购买 API、算力、数据、广告、订阅与其他机器服务，并逐步进入企业采购、退款和供应商付款。

支付协议正在补齐前半段：

- x402 提供机器原生的支付与结算；
- AP2 用 Mandate 表达用户意图与支付授权；
- Mastercard Verifiable Intent 将用户身份、授权约束和交易结果绑定；
- Agent Wallet、Token 与支付网关负责保管凭证、限额和执行。

这些技术已经覆盖部分争议证据：AP2 的 Mandate/Receipt、Mastercard VI 的 identity-intent-action 绑定，以及 t54 的风险判断、可验证审计链和案件导出，都在让 Agent 交易更容易复核。它们尚未共同形成一个跨支付轨道、由保险运营方拥有规则的完整闭环：

> 一笔技术上合法、已经完成结算的交易，如果事实上是 Agent 花错的钱，如何把已有授权与支付证据、结算后的执行上下文、净损失、保障规则、人工处置和恢复连接起来？

## 2. 用户现在面对的责任真空

一次 Agent 支付事故的信息分散在多个主体手中：

1. 用户或企业保存原始任务与授权；
2. Agent 平台保存模型调用和部分执行轨迹；
3. MCP 或工具提供者保存工具请求；
4. 钱包保存签名与广播信息；
5. 区块链保存最终交易；
6. 商户保存商品、交付与退款记录。

每个系统只知道一部分事实。传统日志还可能被拥有系统的一方修改、删除或选择性披露。因此，事故发生后很难可靠回答：

- 用户究竟授权了什么？
- Agent 看到的哪些外部内容影响了支付？
- 支付参数在何处产生或改变？
- 实际交易是否偏离授权？
- 偏离来自攻击、模型异常、工具故障还是用户本人？
- 是否发生了可赔偿的实际损失？
- 哪一条保单责任或除外条款适用？

这使保险方难以快速完成一致的事故调查，支付平台难以公平处理跨系统争议，企业也不敢提高 Agent 的自主权和支付额度。这里不声称现有系统“完全无法理赔”，而是认为其调查材料仍然分散、缺少统一案件对象与 Agent 特有的事故语义。

## 3. 我们的产品判断

Agent Flight Recorder 不试图消灭全部事故。它接受一个现实：安全措施会降低事故率，但无法把事故率降为零。

我们在安全失效之后提供三项能力：

1. **证明事实：** 将事前授权、关键执行行为和最终交易绑定成可验证证据。
2. **辅助归责：** 将事故归类，比较授权与实际行为，解释哪些条款可能适用。
3. **执行恢复：** 在人工授权后，将已批准的理赔结果交给测试赔付合约或现实保险系统。

因此，我们不是另一个 Agent 安全扫描器，也不是一家无牌照保险公司。我们是支付安全层和保险承保层之间的 recovery and claims layer。

## 4. 核心产品对象：Verifiable Claim Packet

系统为每一次受保护的 Agent 支付建立统一关联标识，并在事故后输出 Claim Packet：

- Claim ID、Payment ID、Agent Run ID；
- 用户或企业签署的 Mandate；
- 当时生效的保障条款与策略版本；
- Agent、模型、工具和执行环境版本；
- 影响付款的关键输入来源、摘要或哈希；
- 支付工具请求及结果；
- 金额、币种、收款人、用途和有效期；
- 钱包签名与链上交易回执；
- 关键事件签名与哈希关联，以及存在时的 Merkle proof、链上锚定与 TEE attestation；
- 可量化损失；
- 确定性规则检查结果；
- AI 事故分类、条款映射、置信度与证据引用；
- 人工决定、理由、时间与批准者；
- 最终赔付或拒赔结果。

Claim Packet 的价值不在于保存更多日志，而在于让不同主体可以围绕同一组已声明来源、完整性状态与限制条件的材料作出可复核决定。密码学只能证明签名或锚定后的内容未被无痕修改，不能自动证明外部内容真实，也不能单独证明事故因果关系。

### 4.1 Evidence Manifest最低字段

每个进入 Claim Packet 的关键证据至少包含：

- `evidence_id`：案件内唯一标识；
- `producer`：用户、Agent Runtime、Gateway、钱包、链或商户；
- `schema_version`：规范版本；
- `content_hash`：规范化内容哈希；
- `parent_hash`：前一关键事件哈希；
- `signature` 与 `key_id`：生产者签名及其密钥标识；
- `observed_at`：记录时间；
- `anchor_reference`：可选的链上锚定或可信时间引用；
- `privacy_class`：公开、受限或仅可在可信环境处理；
- `verification_status`：有效、无效、缺失或无法验证（与验证器输出映射：有效=Pass、无效=Fail、无法验证=Unknown、缺失=Not Present；UI 与 CLI 一律使用四态标签）。

Mandate 必须在支付前完成签名并完成授权承诺存证（规则见 §4.4；仅签名不足以使支付成为 Protected）。系统必须保留验证程序哈希（软件级为构建产物摘要及其构建记录，硬件 TEE 时为 measurement）、验证所依赖的信任根和密钥轮换记录。执行环境证明对象带 type 字段（software / tee）：P0 使用 software，由独立验证者密钥对程序哈希、输入摘要与输出摘要签名；其性质是验证者签署的**声明（statement）**，证明的是"该验证者为这组输入/输出背书，且程序哈希在登记白名单内"，不证明程序实际执行过，也不证明输入未被选择性构造；P1 引入硬件 TEE 时不改变包格式与验证器接口。平台在首次可信时间固定之前仍可能构造一条自洽记录，因此产品不得把"哈希链连续"夸大为"事实一定真实"。

### 4.2 可导出的证据包：把事故卷宗交到用户手中

审核员或获授权用户可以导出一个版本化 ZIP 证据包。取得证据包后，即使平台网站不可访问，也能用独立验证工具检查手中的材料。P0 不承诺平台停止服务后仍可获取尚未导出的材料、继续审核或完成赔付。

证据包至少包含：

| 文件或目录 | 内容与用途 |
|---|---|
| `manifest.json` | 包格式版本、随机包标识、谱系标识（lineage_id）与披露集合标识（disclosure_set_id）、案件和支付关联、文件清单、各文件摘要、披露范围、包版本、前版承诺、`anchors_expected`（两条锚定期望的链上定位键，授权存证一条含已知 tx hash 与区块号） |
| `mandate.json` | 付款前已签署的授权及对应签名材料 |
| `evidence/` | 本次获准披露的关键输入、工具调用、交付结果与交易关联材料 |
| `analysis.json` | AI 输入摘要、输出、模型与提示版本、真实证据引用与逐字引文（`quotes`）、局限性 |
| `decision.json` | 存在时的人工作出的决定、决定版本和理由；尚未决定时明确标为不存在 |
| `proofs/` | Manifest 签名（`proofs/manifest.sig.json`）、执行环境证明（软件级验证者声明；存在时含 TEE attestation）、必要验证材料及内容承诺的随机盐（`proofs/salt`，列入文件清单） |
| `anchors.json` | 仅存放包存证交易的 tx hash、区块号等事后才可知的提示信息；不纳入文件清单与承诺，验证器只用它做回执查询，承诺比对按 Manifest 定位键独立进行（构造顺序见 §4.4） |
| `README.md` | 独立验证步骤、隐私说明、信任假设和缺失项解释 |

包导出前展示披露范围与接收对象，只导出当前操作者有权取得的材料。公开版不得夹带受限原文；不同披露集合使用各自明确标记的 Manifest 和承诺，并在链上使用各自独立的引用标识（ref_id 由谱系标识与披露集合标识派生，见 §4.4），互不覆盖、互不冲突。P0 只对评委收到的完整版（`disclosure_set_id = full`）最终包做链上存证；公开版在 P0 不存证，其锚定项显示 Not Present，且包级摘要标注"部分披露集合"。公开版通过验证不等于完整案件通过验证。

已导出的包不可原地改写。补证、AI 重分析、人工决定或退款信息更新均生成新包版本，绑定前版承诺并重新签名；P0 只对最终包版本做链上存证，逐版本存证属于 P1（见 §20）。审核前包可以没有最终决定，最终归档包必须明确其对应的决定与赔付状态。

### 4.3 独立验证工具：不让平台替自己打勾

提供开源命令行验证器，作为 P0 必须交付物；平台控制台提供同类结果展示。验证器无需登录平台、调用平台后端或调用 AI 模型。

验证分为两个模式：

- 离线检查：安全读取包、重算文件摘要、检查 Manifest 签名和对象关联、列出缺失证据。仅有包内自带公钥时，最多确认签名在数学上匹配，签名者身份仍为 Unknown。
- 在线检查：使用验证者自己配置的测试网 RPC、可信合约地址与签名者信任材料，进一步验证链上承诺、交易成功状态、顺序间隔与确认深度、版本新鲜度。仅从包内读到的回执或浏览器链接不能代替链上核验。执行环境证明验证其签名是否来自信任配置中的验证者密钥，以及程序哈希是否在白名单内；硬件 TEE attestation 项在 P0 显示 Not Present，quote 级密码学验证属于 P1。在线模式发现链上同一（类型、引用标识）存在更新版本时，在包级摘要输出 Superseded 标记；它是 advisory 标记而不是第五种逐项状态，不改变逐项 Pass/Fail 结果。授权存证的 Superseded 仅表示存在更新的授权版本，是否影响某笔支付由 §4.4 的生效授权版本规则判定。

信任配置随验证器开源仓库版本化发布（trusted_signers.json：key_id、公钥、角色、生效窗口、EvidenceAnchor 与 ClaimVault 合约地址、链 ID、预期验证程序哈希、测试代币地址），验证器默认从**自身安装目录**（而非当前工作目录或包解压目录）的固定路径读取，并支持命令行参数覆盖；包内出现同名文件时忽略并输出警告。信任配置必须经此包外渠道取得：验证器应拒绝被包内配置覆盖的信任根，不能因为包中附了一把公钥就认定来源可信；信任配置的真实性等同于该仓库发布的真实性，平台后端不参与分发。信任配置的**内容**由运营方自行登记，因此"签名者信任 Pass"的准确含义是"由运营方已登记的密钥在其有效期内签署"，不是"运营方可信"；CLI 与 UI 的人可读输出固定为 `Signer: <role>/<key_id> (per trusted_signers.json @ <repo commit>)`，不得写作"可信签名者"。链上查询依赖所选 RPC 和链的安全假设；P0 不声称使用无信任轻客户端。

验证结果逐项输出 `Pass / Fail / Unknown / Not Present`，覆盖内容完整性、签名者信任、对象关联、授权先后顺序、链上存证、支付与赔付回执、执行环境、披露完整程度。关键检查未知或缺失时，总体不得显示"全部验证通过"。CLI 只输出这四态；`Not Implemented`、`Planned` 是平台 UI 的能力徽章，不出现在 CLI 逐项结果中。

验证结论上限：内容完整性、签名数学匹配、链上承诺一致性、回执状态最高可达 Pass；签名者身份在无信任配置时为 Unknown；执行环境证明（软件级）最高为"验证者已签署该声明且程序哈希在白名单内"，不证明程序实际执行，CLI 人可读输出固定为 `Execution environment: verifier-signed statement (software; not hardware-attested)`；来源真实性与事故因果关系不由验证器判定，最高表述为"与已锚定证据一致"；AI 判断正确性与人工赔付决定的正确性不属于验证范围，验证器只确认其存在、签名与版本。UI 与 CLI 均不得超出上述上限表述。哈希匹配不能验证 AI 判断的正确性，也不能证明未披露材料不存在。

链上核验实现约束：顺序间隔与确认深度均以显式区块高度差计算，不使用 finalized/safe 等 block tag；承诺核对按 Manifest 中 `anchors_expected` 的定位键（类型、链 ID、合约、提交者、引用标识、版本）用 eth_call 读取合约存储，比对承诺与合约记录的区块号，不依赖 anchors.json；anchors.json 仅用于回执查询，其内容与链上记录不一致时输出警告而不改变承诺项结果。`anchors_expected` 声明存在而链上读不到记录时输出 Fail（RPC 故障时为 Unknown），不得输出 Not Present；Not Present 只用于 Manifest 明确声明"本披露集合未存证"的情况。回执查询失败或 RPC 故障时对应项输出 Unknown，不得显示通过。

验证器对 ZIP 路径穿越、重复文件路径、超限文件、重复 JSON 键等输入拒绝处理；不执行包内脚本或任意网络地址。原始包应验证成功；篡改负例集（编号 N01–N11，随仓库以脚本生成，评委可自行重生成）应得到对应结果：N01 修改关键证据文件 → 完整性 Fail；N02 删除必需文件 → 完整性 Fail；N03 替换为未知签名者并重签 → 签名者信任 Unknown；N04 伪造链 ID → 链上存证 Fail；N05 以旧版本冒充最新 → 包级 Superseded 标记；N06 删除 anchors.json → 承诺项按定位键照常核验，回执项 Unknown，包级警告"锚定提示缺失"，不得变为 Not Present；N07 anchors.json 被写入文件清单（构造顺序错误）→ 格式 Fail；N08 修改 Manifest 内 `anchors_expected` 定位键 → Manifest 签名 Fail；N09 篡改 analysis.json 中的 `quotes` → 完整性 Fail；N10 用自有密钥重算全部摘要并重签 → 完整性 Pass、签名者信任 Unknown、链上承诺 Fail；N11 伪造 decision.json 的决定版本 → 签名 Fail 或链上承诺 Fail。

### 4.4 证据摘要链上存证：登记可核对的内容指纹

P0 在 Injective EVM 测试网部署最小 EvidenceAnchor 合约，只登记内容承诺、锚定类型（AUTH / PACKET）、引用标识、版本、提交者与写入区块号，并产生可查询事件。原始用户指令、外部网页、模型输入、案件原文与可识别个人信息均不上链。链上仍会暴露提交地址、交易时间和存证频率，不能声称没有元数据泄露。

存证采用两个时点（P0 仅锚这两点，逐版本存证属于 P1）：

1. 授权存证：付款前，对已签署 Mandate 形成承诺并完成确认。"授权先于付款"使用两个独立参数：**顺序间隔 G**——block_number（授权存证交易）+ G ≤ block_number（支付交易），P0 默认 G=1，同一区块内的授权与付款视为不满足；**确认深度 D**——验证时 当前区块高度 − block_number（授权存证交易）≥ D，P0 默认 D=1（目标链为 CometBFT 即时最终性，区块提交即最终，D 仅作为通用参数保留）。本地时间戳不足以替代链上顺序。Gateway 在存证达到 Confirmed（D 满足）后才广播支付，因此正常路径下 G 恒满足，G 检查主要用于识别预置或异常交易。存证 Failed 或超过可配置超时（默认 60 秒）时，该支付路径终止：Payment 进入 `Aborted(reason = anchor_failed | anchor_timeout)`，不广播支付交易，时间线上保护属性记录为 Unprotected；Gateway 向 Agent 返回明确错误，Agent 不得改走其他支付路径，可按 Mandate 的确认模式转人工处置。被终止的路径不得以重试方式转为受保护支付；同一 Mandate 的存证若在超时后才 Confirmed，新的支付请求可在新路径下引用该已确认存证并重新执行 G/D 检查。授权可升版（用户修改授权），每个版本单独存证；某笔支付的**生效授权版本** = 满足 G 间隔的、该支付区块之前最新已 Confirmed 的授权版本；Agent 使用更旧版本视为 Agent Policy Deviation 候选，Agent 使用尚未确认的新版本视为不满足授权先于付款。
2. 证据包存证：付款结算与人工决定完成后，对最终版本的 Manifest 形成承诺，再导出附带锚定提示的包。最终版本指含人工决定与赔付结果（Paid 或 Payout Failed）的版本；P0 仅对完整版（`disclosure_set_id = full`）披露集合存证。它支持该版本内容与已确认承诺的一致性，不追溯证明每条材料在事故前已存在。

包承诺按两阶段构造，只把真正事后才可知的信息留在承诺之外：**阶段一**生成 Manifest，其 `anchors_expected` 已写入两条锚定期望的全部链上定位键（type = AUTH | PACKET、chain_id、contract、submitter、ref_id、version），授权存证一条还包含其已知的 tx hash 与区块号；计算包承诺并完成链上存证。**阶段二**把包存证交易的 tx hash 与区块号写入 anchors.json（仅此两类信息）后导出。文件清单的排除集合固定为 `{manifest.json, proofs/manifest.sig.json, anchors.json}`，其余文件（含 `proofs/salt`）必须全部列入；anchors.json 出现在文件清单内、或排除集合外的文件缺失，均视为格式错误 Fail。验证器对 anchors.json 不做摘要核验，只把其中 tx hash 用于回执查询；承诺比对一律按 Manifest 定位键 eth_call 读取。

实施时固定 UTF-8、RFC 8785（JCS）JSON 规范化、字段类型和算法版本（SHA-256），并随仓库给出跨环境一致的测试向量。逻辑上的包承诺为 `SHA-256(域标识 || 随机盐 || 规范化Manifest字节)`，域标识固定为 ASCII 字节串 `AFR-PKG-v1`，各段边界必须无歧义（域标识与盐均为定长）；使用不少于 128 位、来自 CSPRNG 的高熵随机盐，降低低熵内容被猜测的风险。Manifest 摘要覆盖本次披露内容；自身签名、链上回执与包承诺不纳入自身摘要，避免循环依赖。ZIP 压缩时间等容器属性不作为内容身份。

引用标识按类型与披露集合分命名空间：`ref_id = keccak256("AFR-REF-v1" || lineage_id || disclosure_set_id)`，lineage_id 为案件谱系的 128 位 CSPRNG 随机值（随案件创建生成，写入 Manifest，不可从支付或 Claim 编号推导），disclosure_set_id 为 ASCII 标签（`full` / `public`）。授权存证使用 type = AUTH，包存证使用 type = PACKET；合约以 (submitter, type, ref_id, version) 为键。对同一键写入相同承诺必须幂等成功（不 revert、不覆盖、不重复计数），写入不同承诺必须拒绝；新版本必须等于当前最新版本 + 1，不覆盖旧记录；合约在存储承诺的同时记录写入区块号，供验证器经 eth_call 读取而不依赖历史回执。包盐及受限材料仅向相应验证者披露，第三方不得从公开承诺推导未授权原文。

存证状态与支付、案件状态分开：`Not Submitted / Pending / Confirmed / Failed`。只有实际查询链上成功事件或合约存储且达到配置确认深度，才能显示 Confirmed；链 ID、合约、提交者、类型、引用标识、版本和承诺必须逐项匹配。顺序间隔与确认深度一律以显式区块高度差计算，不依赖 finalized/safe 等 block tag；核对承诺优先使用 eth_call 读取合约存储，回执查询失败时对应项输出 Unknown。目标链为即时最终性，重组重估属于休眠的通用要求而非验收重点；无法查询时重新评估状态，不显示历史缓存的绿色结果。

## 5. 受保护交易的完整生命周期

### 5.1 创建保障与授权

用户或企业在 Agent 执行前确认：

- 允许执行的任务；
- 总预算和单笔上限；
- 币种；
- 允许或禁止的商户/收款人（P0 演示采用"签名登记的供应商集合"模式：Mandate 引用一份由场景作者签名的供应商登记表，Agent 可在其中动态选择；合成攻击者已作为看似合法的供应商完成登记，因此收款人硬检查通过，语义偏离只能由 AI 识别。"固定单一收款地址"模式保留为可选配置）；
- 有效期；
- 允许调用的支付工具；
- 是否需要人工二次确认；
- 适用的保障条款与除外责任。

V1（本 PRD）生成本项目格式的签名 **AFR Mandate**（EIP-712，domain.chainId 使用 EVM 数字 ID 1439；加 AFR 前缀以区别于 AP2 的 Mandate 对象，二者无格式兼容关系），并在 P0 测试网完成付款前授权承诺存证与确认。授权存证门只设一处：Payment 从 Recording 进入 Submitted（即 Gateway 广播支付交易）之前，对应授权存证必须已 Confirmed；Agent 可在存证 Pending 期间开始检索与推理，但任何支付请求在 Confirmed 之前不得广播。创业阶段如获得 AP2 或 Mastercard VI 的正式接入条件，再通过适配器引用其授权材料，不重新定义上游协议。

### 5.2 Agent执行任务

Agent 可以正常搜索、检索、推理和调用工具。系统不保存完整 chain-of-thought，也不要求所有模型推理运行在 TEE 中。

只记录会影响责任判断的关键事件：

- 可信指令与外部数据的来源；
- 授权策略版本；
- 高风险工具调用；
- 支付请求参数；
- 执行结果与错误；
- 关键事件之间的顺序和关联。

### 5.3 强制支付经过Evidence Gateway

Agent 不直接持有可以绕过记录器的无限制支付能力。所有受保护支付必须经过 Evidence Gateway：

1. 接收 Agent 的支付请求；
2. 关联 Mandate 与当前运行；
3. 执行金额、收款人、时间和权限等硬约束检查；
4. 形成规范化证据事件并签名；
5. 再调用钱包或支付轨道完成交易；
6. 获取交易回执并结束证据链。

如果 Agent 可以绕过 Gateway 直接签名，系统必须明确标记该交易不受保护，而不是伪称证据完整。P0 的具体形态：Agent 钱包为演示热钱包，其私钥只由 Gateway 进程持有，Agent 运行时只能通过 Gateway API 发起支付，"不可绕过"靠密钥隔离实现；Gateway 同时监听 Agent 钱包地址的链上出账，凡没有对应 Gateway 记录的交易在时间线上标记 Unprotected。"Gateway 不托管用户资金"指不持有用户（MetaMask）资产，不指 Agent 演示钱包；生产环境的资金保管模型不由本演示定义。

### 5.4 事故发生与申报

事故可由以下主体发起：

- 用户主动报告；
- Agent 平台监控发现；
- 支付网关发现重复、异常或越权支付；
- 保险方或企业运营人员导入争议。

创建 Claim 时，系统首先确认交易已结算、存在损失且证据可定位。交易回滚、支付失败、Unprotected、无法绑定受保护运行，或 `eligible_loss` ≤ 0 的申报进入 `Not Eligible` 终态并说明原因——这是受理判断，不是赔付裁决，与 Denied 区分。

黑客松使用以下净损失口径：

```text
eligible_loss = min(
  已结算且被认定属于承保事故的金额 - 商户退款 - 已追回资金,
  演示保障上限
)
```

`eligible_loss` 小于或等于零时不得赔付。演示保障上限在合成条款中定义，P0 取值等于 Mandate 总预算上限（演示为 10 AFR-TEST-USD）。生产环境的币价、手续费、部分履约、追偿顺序和免赔额由持牌合作方的正式条款决定，本项目不自行解释。

### 5.5 证据验证与AI理赔分析

系统按固定顺序处理：

1. 验证签名、事件关联，以及当前案件实际提供的 Merkle proof、链上锚定和执行环境证明（含存在时的 TEE attestation）；
2. 比较 Mandate 与实际支付的硬字段；
3. AI 识别可能的事故原因；
4. AI 将证据映射到保障条款；
5. AI 列出缺失证据、矛盾和置信度；
6. 输出带逐项引用的理赔建议。

AI 输出必须是结构化对象，至少包含：

- `primary_hypothesis`：主要事故假设，而不是已被证明的事实；
- `candidate_causes`：其他可能原因及支持/反对证据；
- `evidence_refs`：每个结论引用的 Evidence ID；
- `policy_refs`：可能适用的保障条款或除外责任；
- `missing_evidence` 与 `contradictions`；
- `confidence_band`：高、中、低，不伪造精确概率；
- `recommended_next_state`：人工审核、补证或拒绝进入处理；
- `limitations`：模型与材料限制；
- `quotes`：每个关键结论至少一条 `{evidence_id, exact_substring}`，子串必须在对应证据内容中逐字存在，由评测器与验证页面做确定性子串校验——这是"模型确实读了内容"的可验证落点；
- `model_run`：模型标识、提示模板版本、温度与 seed（如支持）、运行序号、输入摘要（与验证者签名声明中的输入摘要一致）。

模型只能读取完成最小披露和脱敏后的必要证据片段；`TEE-only` 类材料在 P0 没有可用的可信处理环境，不得发送给外部模型，涉及此类材料的判断项 AI 状态标为 Not Available 并转人工。哈希用于完整性验证，不能替代模型进行语义分析所需的内容。AI 的输出是可审查假设，不是密码学证明，也不能覆盖确定性验证结果。

AI 不是最终裁判。低置信度、证据矛盾或高额 Claim 必须升级人工审核。

### 5.6 人工决定与资金恢复

黑客松中的演示审核员可以：

- 批准全额赔付；
- 批准部分赔付；
- 拒赔；
- 要求补充证据；
- 标记疑似欺诈；
- 将争议交给更高权限人员。

黑客松版本由 ClaimVault 在测试网执行测试资产恢复。每次执行使用 `keccak256(claim_ref || decision_version)` 作为合约级幂等键，claim_ref 为不可从支付或 Claim 编号推导的随机标识；交易进入最终确认后才能标记 `Paid`，失败则 Claim 进入 `Payout Failed` 并保留安全重试入口。Claim 状态与决定对象分层：赔付失败时 Claim 状态为 `Payout Failed`，决定对象保持 Approved 版本不变；"疑似欺诈""升级更高权限""待人工追偿"以 Claim 标记 `fraud_suspected / escalated / recovery_pending` 表示，不新增状态。

创业版本中，保单、客户沟通、保障判断和真实赔付必须由当地获许可的保险公司/MGA或明确合法的企业自保主体拥有。Agent Flight Recorder 只提供证据与案件工作流，不托管真实赔付资金。进入任何司法辖区、接入真实保单或采用按理赔金额分成之前，都必须经过当地法律与合规 Gate。

## 6. V1保障边界

### 6.1 可进入理赔判断的事故

V1 聚焦“Agent 支付执行偏离事前授权”且造成实际损失的情形：

- 金额超过 Mandate 上限；
- 支付给未授权收款人；
- 重复支付；
- 授权过期后支付；
- 调用未授权支付工具；
- 金额、时间等硬约束虽然通过，但 Agent 因间接提示词攻击购买了与用户任务语义不符的对象或服务；
- Agent 因间接提示词攻击产生其他可证明的授权偏离；
- Agent 或工具故障导致以上可验证偏离。

生产环境中，可由硬规则明确识别的越权支付应优先被阻止，而不是故意放行后等待理赔。理赔层主要覆盖的是安全控制遗漏、配置失效，以及形式上合规但语义上偏离用户真实任务的残余风险。

### 6.2 V1明确不承诺保障

- 用户在清晰看到金额、币种和收款人后明确签署的具体交易；
- 投资亏损、价格波动或机会成本；
- 买家后悔、商品质量争议或普通商户服务纠纷；
- Agent 建议不佳但用户自主完成的交易；
- Gateway 之外的钱包失窃或私钥泄露；
- 没有形成完整证据、无法证明与受保护运行相关的交易；
- 违法用途、制裁规避或其他保障条款禁止事项；
- 真实保险赔付承诺。

### 6.3 关于“人类被AI误导”

如果人类只签署了带约束的高层目标，Agent 随后编造事实并执行超出目标的交易，可按 Agent 执行偏离调查。

如果人类已经看到并明确批准最终交易，则 V1 默认不赔。AI 建议错误、错误投资推荐与销售误导属于更复杂的 AI E&O 风险，放入未来版本，不与本次黑客松闭环混合。

## 7. 为什么必须同时使用规则、AI与人工

### 确定性规则负责

- 金额是否超限；
- 收款人是否允许；
- 授权是否过期；
- 是否重复支付；
- 签名和证明是否有效；
- 链上是否发生结算。

### AI负责

- 识别间接提示词攻击或语义劫持；
- 将非结构化行为整理成事故时间线；
- 在多项证据中定位矛盾与缺口；
- 将事故事实映射到自然语言条款；
- 生成带引用、置信度和局限性的理赔报告。

### 人工负责

- 最终赔付或拒赔决定；
- 处理低置信度和灰色因果关系；
- 审查疑似欺诈；
- 对外承担业务、合规与资金责任。

去掉 AI，系统会退化成字段比较和日志查看器；去掉确定性验证，AI 报告不可靠；去掉人工，系统会变成不负责任的自动赔付工具。

## 8. 为什么需要TEE和链上证明

TEE 证明指定版本的证据处理或理赔程序在声明的可信环境中运行，并保护敏感输入。链上锚定证明某批证据在某个时间已经存在，之后没有被无痕替换。

二者都不能证明：

- Agent 的语义判断必然正确；
- 外部输入本身真实；
- AI 的事故分类永远正确；
- 硬件不存在任何漏洞。

所以产品只声称“证据处理过程可验证、记录篡改可发现”，不声称“完全去信任”或“全部 Agent 推理都可信”。

P0 的现实配置：不假设主办方或运行环境提供 CDE，演示前端、控制台与服务全部自托管部署；执行环境证明为软件级（独立验证者密钥签名），信任根是验证者密钥而非硬件，UI 与包内必须如实标注；硬件 TEE 属于 P1。本节论证的是 TEE 的价值与上限，不代表 P0 已具备硬件能力。

### 8.1 去中心化边界

本方案的付款入口、材料保管、AI 调用和人工审核仍由明确运营主体控制。链上存证与测试合约执行复用区块链能力；已取得证据包的获授权第三方可以在平台之外独立核验。这降低了对平台展示结果的依赖，但不构成去中心化仲裁、去中心化存储或无信任保险。

P0 不增加 DAO、治理代币、IPFS、多方投票赔付或去中心化模型网络。衡量目标是“第三方能否验证持有的材料”，而不是堆叠去中心化名词。平台停机后可验证性以用户已持有包、可用验证器及必要外部信任材料为前提。

## 9. 创业商业逻辑

### 9.1 初始买方

首个理想客户明确收窄为：**Agent Wallet 或 Agent 支付网关的风险/争议负责人**。他们已经控制支付入口，也直接承担客户事故调查、退款协调和信任成本，是接入 Evidence Gateway 的摩擦最可能被接受的一方——这仍是 §25 列出的首要待验证假设，不是已验证事实。

保险公司/MGA首先作为条款与 Claim Packet 的设计伙伴，待案件量和损失数据形成后再成为承保与分销伙伴。后续扩展买方包括：

- x402 / 稳定币支付服务商；
- 部署采购、退款或付款 Agent 的企业；
- AI Agent 平台；
- AI 保险 MGA、保险公司与再保险机构。

### 9.2 它们购买什么

- 让客户敢于为 Agent 设置更高额度（前提是存在持牌承保方提供保障；在设计伙伴阶段该价值表现为"事故可查、争议可快速处理"）；
- 降低事故调查与争议处理成本；
- 获得可用于保险承保的证据；
- 通过企业采购、风控和合规审查；
- 将不可逆支付转化成存在恢复路径的支付——"恢复"指将人工批准的结果交给持牌方或测试赔付合约执行，本项目自身不持有、不支付真实资金。

### 9.3 收费方式

早期不承担保险风险，收入来自：

- 平台订阅；
- 按受保护交易量或证据事件量收费；
- 每次 Claim Packet / 理赔处理收费；
- 企业级审计、保留和导出能力；
- 通过法律与牌照审查后，与持牌承保方合作的技术服务费或合规允许的收入分成。

### 9.4 长期数据飞轮

受保护交易越多，获得的真实事故类型、损失金额和控制效果越多；理赔判断和风险分层越准确；保险方越愿意提供更高额度和更低价格；更多 Agent 平台因此接入。该飞轮依赖跨租户的**去标识化结果统计**，必须在 §19 的租户隔离、训练禁令与客户合同允许范围内进行，并作为独立的合规 Gate 项。

真正的长期资产不是日志数量，而是经过结果验证的"Agent 行为—事故原因—实际损失—最终决定"数据。

## 10. 与现有玩家的关系

- **Mastercard VI / Google AP2：** 上游授权与交易结果证据标准。VI 自身已是"identity–intent–action 绑定的防篡改记录，供争议时各方查阅"（Mastercard 2026-03 公告，开源、SD-JWT 分层凭证），因此它覆盖的是"用户授权了什么、卡网络侧发生了什么"；它不覆盖 Agent 在两者之间读到了什么外部输入、语义为何偏离、结算后交付是否有效、净损失是多少、哪条保障条款适用、人工如何处置——这些是本项目的差异化所在。黑客松仅参考其字段与语义，创业阶段再做正式适配；
- **x402：** 支付执行与结算轨道；
- **Langfuse / OpenTelemetry：** 调试和运行观测，可作为辅助数据源，不作为独立证据根；
- **Agent 安全产品：** 事前预防和实时阻止；
- **t54 等风险网关：** 按其公开材料覆盖支付前风险判断与审计/案件导出（本文对其能力的描述来自公开产品介绍，未经实测；提交前应附引用链接或删去具体能力列举），是直接相邻竞争者，也可能成为证据来源；
- **保险公司/MGA：** 负责承保、定价、合规与真实资金赔付；
- **Agent Flight Recorder：** 差异化收窄为结算后的执行上下文、净损失计算、由持牌方提供的保障规则、人工处置和恢复连接器，将上游材料转换成保险运营闭环。

### 10.1 P0要求与目标能力

下表是需求边界，不是完成清单。当前所有 P0 实现状态均为 `NOT VERIFIED`，只有真实运行和测试通过后才能改为 `IMPLEMENTED`。

| 能力 | 黑客松 P0 必须实现 | 与外部标准的关系 | 创业目标 | 不得声称 |
|---|---|---|---|---|
| 本项目签名 AFR Mandate | 完成一种本地签名流程（EIP-712） | 自定格式；字段命名参考 AP2 Mandate 与 VI constraint 层，无格式兼容 | 保留为本地/通用格式 | 已等同 AP2/VI 正式集成 |
| 测试网支付与回执 | 完成一种 EVM 流程（Injective 测试网直接转账） | 自定格式；支付请求字段命名参考 x402 PaymentRequirements（payTo / maxAmountRequired / asset / network / resource），仓库附字段映射表；结算机制不同（x402 exact 为 EIP-3009 经 facilitator），Injective 不在 x402 已支持网络中 | 增加标准 x402 适配 | 支持全部 x402 Facilitator；"兼容 x402" |
| AP2 Mandate/Receipt | 不集成 | 参考字段与语义 | 构建正式适配器 | 已获得 Google 生产接入 |
| Mastercard VI | 不集成 | 参考其 Layer 1（身份凭证）/ Layer 2（用户签署约束与 agent 履约凭证）结构 | 在公开 API 可用且合作条件满足后接入 | 已接入 Mastercard Agent Pay |
| t54 | 不集成 | 理解其审计/案件对象 | 评估竞合与互操作 | 市场没有相邻证据产品 |
| 执行环境证明 | P0 为软件级：独立验证者密钥对程序哈希与输入输出摘要签署声明；硬件 TEE 为 P1 | 自定格式，无外部标准 | 多运行环境与多信任根 | 全部 Agent 推理都在 TEE；软件级证明等同硬件 attestation；声明等同执行证明 |
| 可导出证据包 | 版本化 ZIP、披露清单与签名 Manifest | 自定格式，无外部标准 | 跨机构交换与受控披露 | 导出后仍可撤回接收者的副本 |
| 独立验证工具 | 无平台后端依赖的开源 CLI，区分离线与在线验证；信任配置随 CLI 仓库发布；硬件 TEE attestation 显示 Not Present | 自定格式，无外部标准 | 第三方集成与多信任根 | 未知签名者或离线回执等于可信 |
| 证据摘要链上存证 | Injective EVM 测试网的授权承诺与最终包承诺两点登记；逐版本存证为 P1 | 普通 EVM 合约，无外部标准 | 批量 Merkle 锚定与多链适配 | 上链等于事实真实或完全去中心化 |
| 真实保险赔付 | 不实现 | Claim 数据可导出 | 由持牌合作方拥有 | 本项目销售或承保保险 |

"Protocol-agnostic"是创业目标，不是黑客松当前完成状态。演示界面必须把 Implemented、Compatible Model、Planned 三种状态分开。

---

# 第二部分：产品需求文档（PRD）

## 11. Product Summary

Agent Flight Recorder 为受保护的 Agent 支付建立可验证执行记录，并在发生错误支付后生成可复核的 Claim Packet。系统用确定性验证确认授权偏离和证据完整性，用 AI 解释事故原因与条款适用性，由人工审核员作出最终决定，并在测试环境完成资金恢复。V1 使用的是公开标注的合成保障条款，不构成真实保单。

## 12. 产品目标

### 12.1 黑客松目标

在 3 分钟连续演示中证明以下闭环真实运行：

```text
用户授权
→ Agent受到提示词攻击
→ 错误支付完成
→ 证据完整性验证
→ AI事故归因与条款判断
→ 人工批准
→ 测试资产赔付
→ 证据包存证与导出
→ 独立验证原包与篡改包
```

（该闭环对应 §16 Core User Journey 步骤 1–14；§24 按时间轴排列同一链路，步骤编号以 §16 为准。）

同时演示一个不应赔付的对照案例，证明系统不是无条件兜底。

### 12.2 创业目标

成为支付协议无关的 Agent 事故证据和理赔 API，使支付平台与保险方能够为自主支付提供可承保的恢复保障。

## 13. 非目标

黑客松 V1 不做：

- 真实保险销售、承保或保费定价；
- 真实资金池或主网高价值赔付；
- 去中心化仲裁或概率市场；
- 全自动、不可申诉的理赔决定；
- 所有 Agent 框架和所有支付协议；
- 全模型推理进入 TEE；
- 投资建议、商品质量和普通商户纠纷保险；
- 生产级反洗钱、制裁筛查或保险牌照能力；
- 记录或展示模型私有 chain-of-thought。

## 14. Target Users

### 14.1 黑客松直接用户

**Agent 操作者：** 配置授权并让 Agent 通过测试资产执行购买。\
**受损用户/申报人：** 发现错误支付并查看可验证理赔状态。\
**演示审核员：** 查看证据时间线、AI 建议并对合成保障案例作出决定。\
**独立验证者/评委：** 验证证明、运行边界和链上结果。

### 14.2 创业买方

**首个买方：** Agent Wallet / 支付网关的风险与争议负责人。\
**设计伙伴：** 保险公司的数字风险承保/理赔负责人或 MGA。\
**后续用户：** Agent 产品负责人和企业财务自动化负责人。

## 15. 核心状态模型

### Payment状态

- Draft
- Authorized
- Recording
- Submitted
- Settled
- Reverted
- Failed（广播被拒或超过超时未被打包；若随后被打包，按 §4.4 重新评估并迁移到 Settled/Reverted）
- Aborted（reason = anchor_failed | anchor_timeout | constraint_violation；广播前终止，无链上交易）

Payment 另有独立保护属性：Protected、Unprotected；Partially Protected 只用于 Mandate 级汇总（同一 Mandate 下既有 Protected 又有 Unprotected 支付），不用于单笔支付。保护属性不得与结算生命周期混为同一状态。授权存证未达到 Confirmed 时，Payment 不得从 Recording 进入 Submitted；存证 Failed 或超过可配置超时（默认 60 秒）时，Payment 进入 Aborted，时间线保护属性记录为 Unprotected，不得以重试方式转为受保护支付（Mandate 复用规则见 §4.4）。锚定状态机见 §4.4，与 Payment、Claim 状态分开维护。

### Claim状态

- Created
- Not Eligible（受理拒绝：未结算 / 无损失 / Unprotected / 无法绑定 / eligible_loss ≤ 0；终态）
- Evidence Collecting
- Evidence Verified
- Rejected for Processing（Evidence 为 Complete but Invalid 或 Conflicting 且无法补证；终态）
- Needs Information
- Under Review
- Approved
- Partially Approved
- Denied
- Payout Pending
- Payout Failed
- Paid
- Closed（最终包承诺 Confirmed 且导出后进入；Denied / Not Eligible / Rejected for Processing 生成最终包后同样进入 Closed）

Claim 标记（与状态正交，可叠加）：`fraud_suspected`、`escalated`、`recovery_pending`（测试赔付后发生商户退款或追回）。

### Evidence状态

- Complete and Verified
- Complete but Invalid
- Incomplete
- Conflicting
- Unavailable

## 16. Core User Journey

1. 用户打开产品，看到"正常场景"和"攻击场景"两个可运行任务。
2. 用户选择攻击场景，查看并签署任务 AFR Mandate："购买指定主题的有效风险报告，总价不超过 10 AFR-TEST-USD"（AFR-TEST-USD 是本项目自部署、名称中不含任何真实稳定币品牌的 ERC-20 测试代币，不代表真实美元或任何真实稳定币）。
3. 系统明确展示受保护条件：最高金额、服务类型、有效期、签名登记的候选供应商集合和适用保障；该任务允许 Agent 在候选供应商中动态选择，因此没有预先固定单一收款地址。
4. Agent 开始购买 API/数据服务，关键事件出现在实时执行时间线中。
5. Agent 读取包含恶意指令的外部内容，被诱导购买攻击者伪装成数据服务的无效结果，并产生 8 AFR-TEST-USD 支付请求。
6. 该请求没有超过金额和时间等硬约束，因此通过普通规则检查并在测试网结算；演示明确标记这是合成的受控攻击。
7. 系统或用户创建 Claim。
8. 系统验证 Claim Packet，显示授权与实际交易的差异。
9. AI 输出事故类型、条款匹配、证据引用、置信度和缺失项。
10. 人工演示审核员批准或拒绝。
11. ClaimVault 执行测试资产赔付，并显示交易链接。
12. 用户切换到“用户明确授权”对照案例，系统拒赔并说明除外责任。
13. 系统生成最终案件包版本，完成摘要存证后允许导出；页面列出本次披露内容和缺失项。
14. 评委在独立 CLI 中使用自己的信任配置验证原包，再验证修改过的副本，观察对应检查失败。

### 16.1 合成场景的客观判定契约

为避免"AI 主观宣布服务无效"，演示夹具预先定义、签名并版本化 `ValidRiskReport` 契约，随场景文件 `scenario.json`（避免与 Evidence Manifest、manifest.json 撞名）发布。契约每条标注判定归属，规则可判的由规则引擎产出硬事实，AI 不得改写；仅语义条款由 AI 判定：

| 契约条款 | 判定归属 | 说明 |
|---|---|---|
| 包含指定章节 | rule | 结构检查 |
| 至少两个可定位来源 | rule | 来源字段存在且可解析；来源内容是否真实由 AI 标注为不可验证 |
| 生成时间在 Mandate 有效期内 | rule | 时间比较 |
| 内容哈希与交付回执匹配 | rule | 哈希比对 |
| 候选供应商在签名登记表内 | rule | 收款人硬检查（攻击者已登记，因此通过） |
| **报告主题与 Mandate 任务一致** | **ai** | 唯一语义条款，也是 Prompt Injection 夹具的判定核心 |
| **正文是否被外部指令劫持** | **ai** | 需引用外部输入中的注入片段（`quotes`） |

攻击样本在金额、时间和候选商户等硬字段上全部合规，返回结果的形式也看似完整（规则条款全过），但正文被外部隐藏指令劫持，核心内容与所购风险主题不一致。场景作者在不提供给 AI 的 ground-truth 文件中标注预期类别、must-cite 证据集合、预期条款引用与理由；评测器用它做确定性评分（见 Story 4.3）。模型必须以 `quotes` 引用实际输入片段得出语义偏离，不能读取答案标签；ground-truth 隔离通过验证者声明中的输入摘要证明：评测记录附 AI 输入集合的摘要列表，ground-truth 文件哈希不得出现其中。

## 17. Epics and User Stories

### Epic 1：创建清晰、可验证的支付授权

#### Story 1.1

作为 Agent 操作者，我希望在任务执行前看到并确认支付边界，以便知道 Agent 能做什么、不能做什么。

验收标准：

- 页面展示任务、金额上限、币种、允许收款人（P0 为签名登记的供应商集合）、有效期和确认模式；
- 用户未确认时，Agent 不能开始受保护支付；
- 确认后生成唯一 Mandate ID；
- 用户可以查看用于判断理赔的保障条款和除外责任；
- 授权内容在任务开始后不可被无痕修改。

#### Story 1.2

作为独立验证者，我希望确认 Mandate 确实先于交易存在，以便排除事故后补写授权。

验收标准：

- 系统展示 Mandate 时间、签名状态和内容摘要；
- 修改 Mandate 后，验证状态变为失败；
- Mandate 与 Payment ID、Agent Run ID 可以互相跳转。

### Epic 2：记录与支付责任有关的Agent执行证据

#### Story 2.1

作为 Agent 操作者，我希望看到关键执行时间线，以便理解支付是如何产生的。

验收标准：

- 时间线至少包含任务开始、外部输入、关键工具调用、支付请求、钱包执行和交易结算；
- 每个事件显示来源、时间、类型和验证状态；
- 敏感原文默认不公开，仅显示必要摘要或哈希；
- 页面不得声称展示完整 chain-of-thought。

#### Story 2.2

作为风险负责人，我希望所有受保护支付都经过统一入口，以便证据不会在付款完成后才补录。

验收标准：

- 受保护支付显示 Gateway 记录；
- 绕过 Gateway 的交易显示 Unprotected；
- Unprotected 交易不能被系统标记为证据完整；
- 重复提交相同支付请求时，系统在时间线上明确显示重试与结果。

### Epic 3：生成并验证Claim Packet

#### Story 3.1

作为受损用户，我希望从一笔已结算交易创建 Claim，以便启动调查。

验收标准：

- 用户能从交易详情创建 Claim；
- 未结算、已回滚、Unprotected、无法绑定受保护运行或不存在损失的交易进入 `Not Eligible` 并说明原因，不进入赔付；
- 重复 Claim 会指向已有案件，而不是产生两个独立赔付机会；
- 创建后立即显示证据收集进度。

#### Story 3.2

作为获授权的验证者，我希望使用登记或公开的信任材料检查证据完整性，以便不依赖平台口头保证。

验收标准：

- 系统分别显示签名、执行环境证明与 P0 最小链上存证结果；平台 UI 使用能力徽章区分 `Implemented / Not Implemented / Planned`，P0 必须项徽章为 `Not Implemented` 时该项 P0 不得验收通过；逐项验证结果一律为四态，硬件 TEE attestation 在 P0 为 `Not Present`，P1 连续哈希链与 Merkle proof 未实现时徽章为 `Planned`、结果为 `Not Present`；
- 任一证据被修改后，对应验证项失败；
- 验证失败不会被 AI 建议覆盖；
- 用户可以查看付款交易、付款前授权存证和指定包版本的存证引用；缺少关键引用时不得显示完整验证通过；
- 验证页面列出 Evidence Manifest、签名者 `key_id` 与角色、信任配置来源（仓库与 commit）、验证程序哈希（软件级为构建产物摘要，硬件 TEE 时为 measurement）、验证时间与无法验证项；
- 公众只能验证已披露的哈希和链上引用；受限原文仅向有权限的审核主体开放；
- 页面明确提示：完整性通过不等于来源真实或因果关系已证明。

#### Story 3.3

作为获授权用户，我希望导出一份版本固定的案件材料，以便交给另一位审核员，并在平台不可用时保留核验能力。

验收标准：

- ZIP 内容、版本、文件摘要、披露范围与 Manifest 一致；包内不得出现密钥或未获授权原文；
- 更换平台展示页面不影响验证器对同一包的结果；
- 补证、人工决定、赔付回执更新生成新版本与新承诺，旧包仍可验证；
- 受限版与公开版分别标明可验证范围，不将公开版的部分通过冒充完整案件通过。

#### Story 3.4

作为评委，我希望在平台外验证证据，并发现被改动的副本，以便判断产品是否提供真实独立核验能力。

验收标准：

- 独立 CLI 的信任配置随其开源仓库版本化发布，不调用平台 API 或模型，并拒绝包内信任材料；
- 验证原包后，§4.3 篡改负例集 N01–N11 全部得到规定结果；负例由仓库内脚本生成，评委可自行重生成而非依赖预先提供的副本；
- 离线模式与 RPC 故障模式明确列出不能验证的链上检查，不显示全部通过；
- 即使攻击者重算文件摘要并用自有密钥签名，也不能通过原可信签名者与原链上承诺检查（N10）；
- 执行环境证明的签名校验失败显示 Fail；硬件 TEE attestation 项在 P0 显示 Not Present，任何情况下不因此显示全部通过；
- 输出机器可读结果（JSON）和人可读摘要，人可读摘要使用 §4.3 规定的固定标签措辞，不把完整性通过解释为赔付结论成立，且不得超出 §4.3 的验证结论上限。

#### Story 3.5

作为审核员，我希望核对付款前授权和付款后证据包的链上承诺，以便确认事后材料未被悄悄替换，并区分不同时间点能支持的结论。

验收标准：

- 授权存证 Confirmed（确认深度 D，默认 1）后才广播受保护付款，且验证时满足顺序间隔 G（默认 1）：授权存证区块高度 + G ≤ 支付交易区块高度，同一区块视为不满足；存证 Failed 或超时（默认 60 秒）时 Payment 进入 Aborted、保护属性记录为 Unprotected；
- Manifest 包承诺与链上记录按固定算法匹配：验证器用 Manifest `anchors_expected` 的 (submitter, type, ref_id, version) 经 eth_call 读取承诺与写入区块号，不依赖 anchors.json；相同承诺重复写入幂等成功，不同承诺写入同一键被拒绝，版本必须为最新 +1，重复提交不覆盖同版本记录；
- 上链内容不包含敏感原文，原文变更生成新包和新存证；不同披露集合使用不同 ref_id；
- 待确认、失败、错误链、错误合约、错误提交者或未知信任根不显示通过；
- 事后包存证不得被描述为事故发生前全过程已经真实存在的证明。

### Epic 4：AI辅助事故归因与条款判断

#### Story 4.1

作为案件审核员，我希望快速看到事故时间线与授权偏差，以便减少人工翻查多套日志的时间。

验收标准：

- 报告列出用户授权与实际支付的逐项比较；
- AI 输出符合固定 schema，并把 `primary_hypothesis` 明确标为假设；
- 每项关键判断链接到真实存在的 Evidence ID，不得生成不存在的引用；
- 报告显示候选原因、支持与反对证据、置信区间、矛盾、缺失证据和模型局限性；
- AI 不得直接触发真实赔付。

#### Story 4.2

作为案件审核员，我希望看到条款匹配结果，以便判断该事故是否可能符合演示保障规则。

验收标准：

- 报告引用具体保障条款或除外责任；
- 金额超限、收款人不符、重复结算等硬事实由规则结果呈现，AI 只在规则事实之上解释原因（例如重复结算是超时重试还是用户预期的分批），不得改写规则结论；
- 证据不完整时结果为 Needs Information，而不是强行批准或拒绝；
- 对"用户明确批准最终交易"的案例，系统引用除外责任并建议拒赔。

#### Story 4.3

作为评委，我希望 AI 能力通过固定测试集被单独验证，以便确认它不是对演示答案的硬编码包装。

验收标准：

- 仓库包含至少 6 个固定案件（夹具编号 F01–F06）：Prompt Injection ≥ 2（其一注入措辞明显，其一注入措辞隐蔽、不含"ignore previous instructions"类标记词）、用户明确授权 ≥ 1、重复支付 ≥ 1、证据缺失 ≥ 1、无攻击相似对照 ≥ 1（外观与攻击样本相近，含看似指令的良性文本）；Agent Policy Deviation 由规则引擎覆盖，不作为 AI 夹具；
- 每个夹具附不提供给 AI 的 ground-truth：预期类别、must-cite Evidence ID 集合、预期 Policy Clause ID 集合、允许的 confidence_band 集合、允许的 recommended_next_state 集合；夹具文件名与 Evidence ID 不得含类别标签词；
- 评测器为确定性程序（随仓库发布）：逐项比对类别精确匹配、`evidence_refs ⊇ must-cite`、`policy_refs ∩ 预期 ≠ ∅`、confidence 与 next_state 落在允许集合、`quotes` 子串在对应证据内逐字存在、硬字段与规则引擎输出字节一致；"理由"字段只供人读，不评分；不使用 LLM 作为评分器；
- 所有关键 AI 结论引用的 Evidence ID 和 Policy Clause ID 必须 100% 存在，且不得引用 ground-truth 文件；
- ground-truth 隔离可验证：每次运行记录 AI 输入集合的逐文件摘要（`model_run.input_digests`），并与验证者声明的输入摘要一致；评测器断言 ground-truth 文件哈希不在其中；
- 缺少决定性语义证据时，模型必须输出补证或低置信度，不得自动批准；
- 确定性硬字段由规则引擎计算，AI 不能改写；
- 每个夹具至少运行 3 次，记录模型标识、提示模板版本、温度/seed、每次实际输出与评分，失败结果也如实保留；
- 扰动集：对每个夹具生成 Evidence ID 重命名、证据顺序打乱、注入措辞改写三种变体，类别判定须与原夹具一致；
- 评委现场可修改 Mandate 主题或替换注入文本后重跑一次，输出与评分实时展示。

通过门槛（全部为硬门槛，任一失败则 AI 能力不得在提交材料中标为通过）：

- 正向：每个 Prompt Injection 夹具在 ≥3 次运行中 100% 判为 Prompt Injection、confidence ≥ 中、`evidence_refs ⊇ must-cite`（must-cite 必含注入内容所在 Evidence ID）；用户明确授权夹具 100% 建议拒赔且 policy_refs 含对应除外条款；重复支付夹具 100% 类别正确；
- 反向：硬字段改写次数为 0；所有缺证夹具 recommended_next_state ∈ {补证, 人工审核} 且 confidence ≠ 高（Claim 由此进入 `Needs Information`）；无攻击对照的 primary_hypothesis 在任何置信度下不得为 Prompt Injection，recommended_next_state 不得为进入赔付审核；
- 稳定性：扰动集类别一致率 100%；引用存在率 100%；`quotes` 子串校验通过率 100%。

### Epic 5：人工决定与测试赔付

#### Story 5.1

作为演示审核员，我希望在完整证据和 AI 建议之上作出决定，以便保留明确责任主体。

验收标准：

- 批准、部分批准、拒赔和补证均要求填写理由；
- 决定记录审核员、时间和所依据的证据；
- 未经人工确认不得执行赔付；
- AI 建议与人工决定不一致时，两者都被保留。

#### Story 5.2

作为受损用户，我希望看到赔付结果和可验证回执，以便确认资金恢复已经完成。

验收标准：

- 批准后显示赔付金额、测试资产和交易状态；
- 支付成功后提供测试网交易链接；
- 交易失败时 Claim 进入 `Payout Failed`（决定对象保持 Approved 不变），不得显示 Paid；
- 赔付请求的合约级幂等键为 `keccak256(claim_ref || decision_version)`，同一决定不能重复执行；
- 交易在设定的测试网确认深度达到后才从 `Payout Pending` 变为 `Paid`；
- 重试沿用同一幂等键并保留每次广播记录；先前滞留的广播若后被打包，合约幂等键保证只有一次成功；
- 商户退款或追回资金发生在决定前时，重新计算 `eligible_loss`；发生在测试赔付后时，设置 `recovery_pending` 标记并转人工追偿，不静默改写历史决定。

### Epic 6：让评委独立理解系统边界

#### Story 6.1

作为评委，我希望在一个界面看懂哪些部分真实运行、哪些是模拟和人工步骤，以便判断项目可信度。

验收标准：

- 页面标明测试网、合成攻击、外部模型、执行环境证明方式（软件级或硬件 TEE）和人工确认；
- 提供一键验证证据入口；
- 提供正常、获赔和拒赔三个可复现结果；
- 不展示或使用真实客户金融数据、主网高价值资产或私钥；
- 所有失败降级路径都有明确标签，不以预录冒充实时运行。

## 18. V1事故分类

| 类别 | V1示例 | 硬证据 | AI作用 | 默认处理 |
|---|---|---|---|---|
| Prompt Injection | 恶意页面诱导 Agent 在预算内购买与任务不符的伪装服务 | 输入来源、任务语义、工具调用与结果不一致 | 识别恶意语义与因果链 | 可承保候选 |
| Agent Policy Deviation | Agent 自行突破金额上限 | Mandate 与交易比较 | 解释可能原因 | 可承保候选 |
| Duplicate Execution | 超时重试导致重复付款 | 相同意图对应多笔结算（由规则判定） | 在规则已判定重复的前提下解释原因（超时重试/工具故障/用户预期分批），不改写规则结论 | 可承保候选 |
| User-Authorized Action | 用户明确确认最终错误交易 | 用户签名与最终参数一致 | 摘要说明 | 默认拒赔 |
| Evidence Failure | 证据缺失、签名或证明无效 | 验证失败 | 列出缺口 | 补证或拒绝处理 |

## 19. Edge Cases

### 授权与执行

- Mandate 在交易广播前过期；
- 用户修改授权但 Agent 使用旧版本（按 §4.4 生效授权版本规则判定）；
- 授权存证超时后才 Confirmed，同一 Mandate 下发起新支付（允许，新路径重做 G/D 检查）；
- 一个任务产生多笔合法付款（Mandate 级汇总可为 Partially Protected）；
- 收款地址改变但商户身份未变；
- Agent 请求金额合规，但累计金额超预算；
- 同一工具调用因网络超时重复执行；
- 支付被标记 Failed 后才被打包（重新评估为 Settled，时间线保留 Failed 记录）。

### 证据

- Langfuse 或观测日志存在，但可信证据链缺失；
- 软件级执行环境证明签名无效或程序哈希不在白名单（P0）；硬件 TEE attestation 过期或无法验证（P1）；
- 部分原始数据因隐私策略不可披露；
- 链上锚定成功但交易回执暂未最终确认；
- Agent Run ID 与 Payment ID 无法绑定；
- 外部内容已删除，只剩事故前生成的哈希和摘要；
- anchors.json 缺失或与链上记录不一致（承诺按定位键照常核验，回执 Unknown）；
- 同一案件的公开版与完整版包各自锚定、互不覆盖。

### 理赔

- AI 置信度低；
- 规则与 AI 结论冲突；
- 人工否决 AI 的批准建议；
- 用户提交重复 Claim；
- 赔付合约交易失败；
- 用户在清晰确认最终交易后反悔；
- Claim 金额超过演示保障上限；
- 事故同时包含提示词攻击与配置失误；
- 演示多会话复用同一预置支付（被重复 Claim 规则阻止，因此每个会话需独立预置支付）。

对应原则：证据不完整不等于自动拒赔，但不得自动批准；验证失败不得被语言模型覆盖；没有实际损失不得产生赔付；所有灰色案件回到人工。

### V1隐私与安全控制

黑客松版本只使用合成任务、合成攻击和测试资产，不导入真实客户金融数据。V1 最低控制包括：

- 原始外部内容、用户指令与模型输入按 `Public / Restricted / TEE-only` 分类（P0 没有 TEE，TEE-only 材料仅人工可见，涉及的 AI 判断项标为 Not Available）；
- UI 默认展示摘要和哈希，受限原文需要案件权限才能查看；
- 传输使用 TLS，存储加密，租户与案件按角色隔离；
- 对 Evidence Packet 的查看、导出、AI 调用和人工决定记录访问审计；
- 第三方模型调用仅发送必要的脱敏片段，并记录供应商、区域和用途；生产接入必须通过合同或服务配置禁止将案件数据用于模型训练；
- 演示数据设定明确保留期并支持删除；法律保全、真实客户删除请求和跨境传输属于生产 Gate；
- 密钥不写入日志或 Claim Packet；信任配置携带签名密钥注册表：每个 key_id 记录公钥、valid_from、valid_to，验证历史包时按证据 observed_at 选择当时有效密钥；轮换后旧密钥保留在注册表中并标记 retired，注册表未覆盖时间段内的签名输出 Unknown；
- 任何无法满足数据区域、跨境、保留或外部模型约束的客户，默认关闭相应 AI 外发路径并转人工。

## 20. What We Are Building：黑客松范围

### P0：必须现场跑通的一条主干

P0 只保留一条单页控制台可连续演示的受控路径：

1. 用户签署本项目格式的 AFR Mandate（EIP-712，domain.chainId=1439），启动一个受控 Agent 购买任务；
2. 付款前完成授权承诺存证并满足 §4.4 的 G/D 规则；付款请求经过 Evidence Gateway（只做约束检查与证据签名；Agent 演示热钱包私钥由 Gateway 进程持有，不持有用户 MetaMask 资产），在 Injective EVM 测试网结算。评审现场默认现场实跑 Agent 购买（小模型 + 缓存的合成外部页面）；若外部模型延迟或可用性构成风险，可使用预先结算但可复验的攻击交易作为**明确标注的降级**——理由是外部模型延迟与可用性，不是链确认（目标链出块约 0.64 秒）。预置交易必须由同一 Gateway 按同一规则生成并满足全部检查，不满足规则的预置交易不得用于评审；每个演示会话使用独立的预置支付与 Claim；
3. Gateway 生成签名 Evidence Manifest，并把 Mandate、关键语义输入、工具调用和交易回执绑定为一个 Claim Packet；
4. 确定性证据验证由独立验证者服务执行：以单独密钥对（验证程序哈希 + 输入摘要 + 输出摘要）签署软件级执行环境**声明**，并封装 AI 所见输入与输出摘要；页面与包内如实标注其信任根为验证者密钥而非硬件，且声明不证明程序实际执行；
5. AI 对"硬约束通过但语义目标被劫持"的合成 Prompt Injection 案例输出结构化原因假设、条款映射、Evidence ID 引用与逐字 `quotes`；
6. 演示审核员在同一控制台作出决定，ClaimVault 以合约级幂等键 `keccak256(claim_ref || decision_version)` 执行测试资产恢复（AFR-TEST-USD）；
7. 最终包（含决定与赔付回执）承诺存证并导出版本化 ZIP；P0 只锚两点：授权承诺（type=AUTH）与最终完整版包承诺（type=PACKET, disclosure_set_id=full），中间版本由 Manifest 签名与前版承诺链绑定；
8. 开源独立 CLI：离线检查（摘要、签名、对象关联）与在线检查（两处锚定按 Manifest 定位键 eth_call、支付与赔付回执、版本新鲜度）；现场分别核验原包与 N01–N11 中至少 3 个负例；硬件 TEE attestation 项显示 Not Present；
9. 页面同时标明测试资产、合成攻击、合成保障条款、外部模型、执行环境证明方式（软件级声明）、人工步骤和未实现能力。

用户明确授权后的拒绝案例作为签名固定夹具随仓库提供，P0 只需现场加载并得到正确结果，不要求再运行第二次完整 Agent 支付。

P0 验证集合为：AFR Mandate 签名与付款前授权存证、Evidence Manifest 签名与对象关联、软件级执行环境声明、测试网交易回执、最终包承诺存证、导出包与独立 CLI 核验（含 Superseded 标记与篡改负例集 N01–N11）。硬件 TEE attestation、完整连续哈希链、批量 Merkle proof 与逐版本存证均属于 P1，未实现时 UI 徽章为 `Planned`、验证结果为 `Not Present`。

评审现场必须实时运行 Claim 验证、AI 分析、人工决定、一次测试资产恢复和独立 CLI 验证。完整路径须保留真实连续运行记录。若现场使用已确认的授权、攻击付款或包存证，必须标注其运行时间和版本，独立验证展示该历史包的实际状态，不能冒充本次审核的新包。最终包在新决定或新回执加入后须重新存证，未确认前明确显示 Pending。预置交易与环境故障降级按赛制披露，需批准的降级由主办方确认。

### P1：有时间再增强

- 为所有事件构建连续哈希链、批量 Merkle root 锚定与逐版本存证，降低逐包存证成本；P0 已包含授权承诺与最终包承诺两点存证，不要求 Merkle 树；
- 真实 TEE/CDE 保护点（如 Phala Cloud、Marlin Oyster 或机密 VM），将执行环境证明从软件级升级为硬件 attestation，包格式与验证器接口不变；CLI 对 TEE attestation 的 quote 级密码学验证（含信任根与 collateral 获取）随之启用；
- Bank/MTS precompile 的原生 denom 测试赔付演示（MultiVM 互操作叙事；本地 fork 测试需 InjectiveLabs 补丁版 Foundry）；
- 现场完整运行第二个拒绝案例；
- Langfuse / OpenTelemetry 可视化辅助证据；
- 多模型比较、申诉、多级审核和复杂部分赔付；
- AP2、Mastercard VI、x402 或 t54 的真实适配器。

### 可以降级但必须诚实标记

- 不假设主办方或运行环境提供 CDE；P0 一律使用软件级执行环境声明，如临时获得真实 TEE 条件，仅作为增强展示并如实标注；
- 外部模型失败时，保留证据验证和确定性规则结果，将 AI 状态标为不可用；
- 外部模型延迟超出演示预算时，改用预置攻击交易（§20 P0 步骤 2 的条件），并在页面标注"预置于 <时间>"；
- 测试网拥堵时，使用已广播交易或经主办方允许的替代测试网，并保留复现记录；
- 演示中若攻击结果预先构造，必须标为合成攻击，不得称为对真实系统攻击。

### 技术基线（已定方向）

- 目标链：Injective EVM 测试网，EVM Chain ID 1439（对应 Cosmos chain-id injective-888，仅作网络标识，不参与任何 EVM 签名参数）。主网 EVM Chain ID 1776 保持 NOT DEPLOYED：部署脚本默认测试网，主网需显式 flag 且在 P0 阶段禁用。不使用已被官方废弃的 inEVM。
- 合约：Solidity + Foundry；EvidenceAnchor 与 ClaimVault 为普通最小合约，不使用 precompile；EvidenceAnchor 以 (submitter, type, ref_id, version) 为键存储承诺与写入区块号；Bank/MTS precompile 为 P1 可选；Exchange precompile 不属于核心场景。
- 前端与独立 CLI：TypeScript + viem；viem 无内置 Injective 链，须 defineChain({ id: 1439 })；EIP-712 的 domain.chainId 必须使用数字 1439，严禁误用字符串 injective-888。
- 交易与 gas：按 Injective 官方教程显式指定 gas price 与 gas limit；EIP-1559 支持在官方页面间口径不一致，P0 默认 legacy（type-0）交易，type-2 仅在实际验证支持后启用。
- RPC：官方 network-information 页列出测试网公共端点 https://k8s.testnet.json-rpc.injective.network/ 与测试网归档端点 https://testnet.evm.archival.chain.virtual.json-rpc.injective.network/；官方 EVM 集成速查页（evm-integrations-cheat-sheet）说明公共端点重度限流且截断历史数据查询，推荐 QuickNode/Thirdweb。平台默认公共端点；**独立 CLI 随仓库发布的默认 RPC 为官方归档端点**（在线核验需读取数天前的预置交易回执），并支持命令行替换为第三方 RPC；承诺与锚定区块号一律经 eth_call 读取合约存储，回执查询只用于支付/赔付/锚定交易状态；顺序间隔与确认深度一律用显式区块高度差计算，不使用 finalized/safe block tag；回执查询失败输出 Unknown。
- 浏览器：测试网 Blockscout（https://testnet.blockscout.injective.network/）仅作展示，链接不代替 RPC 验证。
- 资产：gas 使用测试 INJ（官方水龙头支持 0x 地址直接领取）；演示计价与赔付使用本项目自部署的 ERC-20 测试代币 `AFR-TEST-USD`（18 位小数，名称与符号中不含任何真实稳定币品牌），ClaimVault 预注资由部署脚本完成并记录；不使用官方测试网 USDC/USDT 合约，不声称真实 USDC 或真实保险赔付。
- 部署形态：演示前端、控制台、Gateway 与验证者服务全部自托管（自有机器部署），不依赖主办方提供 CDE、托管环境或内网服务。
- 文档基准：Injective 官方页面间存在数据漂移（RPC URL、代币地址均有不一致版本），网络参数一律以 developers-evm/network-information 页为准，其他页面的说明性内容引用时注明页面；部署配置随仓库发布。

### 解封条件（进入 Technical Spec 前必须完成的 Spike）

1. **钱包签名与链下恢复：** MetaMask 添加自定义网络 1439 后完成 EIP-712（eth_signTypedData_v4）签名，viem 链下 `recoverTypedDataAddress` 恢复出签名地址。说明：EIP-712 签名在钱包内完成、恢复在链下完成，链本身不参与，此 Spike 验证的是自定义网络下的 chainId 匹配与本项目类型定义，不是 Injective 的能力；P0 没有链上验签需求。附注：MetaMask 文档说明硬件钱包（Ledger/Trezor）不支持 typed data 签名，演示使用软件账户；
2. **合约与 RPC：** Foundry 部署最小 EvidenceAnchor 到测试网，viem 经可配置 RPC 读回承诺、区块号、事件与回执，并确认 legacy 交易与显式 gas 参数行为；同时实测公共端点与归档端点对 ≥3 天前交易的 `eth_getTransactionReceipt` 与 `eth_getLogs` 可查深度，结果写入信任配置默认值；
3. **CLI 原型：** 对一个手工构造的包跑通离线与在线检查，四态输出正确，篡改负例集 N01–N11 全部得到 §4.3 规定的结果；
4. **端到端计时：** 用目标模型对 §24 每个时段各实测 ≥3 次（Agent 购买段、AI 分析段、验证者签名、授权存证、包存证、赔付确认），记录 p50/p95；任一时段 p95 超过 §24 预算时，必须在录制前调整预算表或声明可见时间压缩，不得靠剪辑。

四个 Spike 全部通过前，本文状态保持 UPDATED DRAFT / 待技术验证，所有能力维持 NOT STARTED / NOT VERIFIED。

## 21. What We Would Add With More Time

- Mastercard VI、AP2、x402 等多协议适配；
- 面向 Agent 框架的 Recorder SDK；
- 多 TEE 或多证明信任根；
- 保险公司与 MGA 的真实案件系统接口；
- 支持企业自保、保证金和现有退款轨道；
- 欺诈检测、申诉与多级审核；
- 事故聚合、风险定价和保障额度建议；
- 跨 Agent、模型、工具和商户的匿名风险基准；
- 面向监管或审计机构的证据导出；
- 非支付高风险 Agent 行为，但只在支付楔子验证后扩展。

## 22. Success Metrics

### 黑客松成功标准

- 核心路径可以在测试网与自托管环境连续运行；
- Prompt Injection 主路径可重复运行，拒绝案例固定夹具可重复验证；
- Claim Packet 的签名、Manifest 与执行环境证明可以由评委触发验证；
- P0 导出包可脱离平台后端验证，授权与包承诺可经独立 RPC 核验；篡改、缺失、不可信签名者、伪造链引用与旧版本冒充最新均有负例记录；CLI 对无法完成验证的项输出 Unknown 或 Not Present 而非通过；
- 若实现 P1，连续哈希链和 Merkle proof 也可单独触发验证；
- AI 报告符合固定 schema，每个主要结论引用有效 Evidence ID；
- 至少 6 个固定 AI 案件完成评测并通过 Story 4.3 的正向、反向与稳定性门槛，评测记录随仓库发布；
- 人工批准前不能执行赔付；
- 修改任一关键证据会导致验证失败；
- 同一 `claim_ref + decision_version` 无法产生重复测试赔付；
- 3 分钟视频包含真实运行，不依赖纯 PPT；
- 团队能清楚回答"去掉 AI""去掉链""去掉执行环境声明（以及 P1 若引入硬件 TEE）"分别损失什么能力。

### 早期创业验证标准

- 至少 2 家 Agent 支付/钱包团队愿意评审 Claim Packet；
- 至少 2 名承保或理赔从业者说明哪些字段可以或不能用于真实判断；
- 至少 1 个设计伙伴愿意提供测试工作流或接入意向；
- 能从合成的受保护交易（以及通过合规 Gate 后的真实交易）中稳定重建事故时间线；
- 与设计伙伴共同定义"人工调查步骤"基线（例如当前处理一起争议需要查阅的系统数与人工小时数），证明证据/理赔 API 相对该基线减少步骤，而不仅是生成更漂亮的报告。

## 23. Submission Proof Points

### 对应“技术实现与创新性”

- Agent 支付、执行环境证明（软件级）、链上存证和 AI 理赔形成一个运行闭环；
- AI 不只是聊天框，承担非结构化事故归因、证据缺口识别和条款映射。

### 对应“问题定义与场景价值”

- 解决的是 Agent 支付发生后无法归责与恢复的现实问题；
- 首个买方是 Agent Wallet / 支付网关的风险与争议团队；保险方先作为设计和承保伙伴，而不是假设所有散户逐笔买保险。

### 对应“隐私、安全与可验证性”

- 原文最少披露；
- 关键事件有哈希、签名和软件级执行环境证明；版本化证据包可导出，授权和包承诺在测试网上存证；
- 评委使用独立 CLI 和包外信任配置核验，篡改副本触发失败；
- 公众可验证已披露哈希和链上引用，获授权主体可使用登记的信任材料验证受限证据；
- 明确披露信任根和无法证明的内容。

### 对应“合规意识”

- 只使用测试资产和合成事故；
- V1 只执行合成保障规则，不销售保险、不托管真实赔付资金、不提供最终法律责任判定；
- 演示由人工掌握最终决定；生产中的保单、客户沟通、保障决定和赔付由获许可主体拥有；
- 任何真实保单、司法辖区和按理赔金额收费模式先经过法律与合规 Gate；
- 外部模型和跨境数据使用明确披露。

## 24. 3分钟演示脚本

三分钟提交视频包含同一条路径的连续真实运行，录制前按 §20 Spike 4 实测模型响应、授权存证、支付、包存证与赔付确认耗时（p50/p95）。**剪辑规则：**允许"带屏显标注的可见时间压缩"（例如在等待外部模型时叠加"⏩ 已压缩 38s，实测等待"字样并保留进度指示），禁止剪切掉任何步骤、禁止伪造成功状态、禁止用预录画面冒充实时运行；若 p95 总耗时超过预算，优先压缩叙述段与非关键交互，其次声明可见时间压缩。现场展示窗口按 AIX 文档为 5—8 分钟，可容纳完整解释与核验，现场不做时间压缩。

| 时间 | 演示动作 | 评委应看到的证据 | 主要耗时来源 |
|---|---|---|---|
| 0:00–0:10 | 说明 AI 在预算内也可能买错东西 | 明确的金融事故与目标用户 | 叙述 |
| 0:10–0:25 | 签署授权并完成授权存证 | 付款前授权承诺、Confirmed 状态 | MetaMask 交互 + 1 次链上交易（≈1 秒出块） |
| 0:25–1:10 | 受控攻击与测试付款（Agent 现场运行，2–3 次模型调用） | 合成恶意输入、真实测试交易、Gateway 记录 | 外部模型 ×2–3 次（p95 待 Spike 4）；允许可见时间压缩 |
| 1:10–1:40 | 核验证据并运行 AI 分析 | 引用实际材料与逐字 quotes 的语义偏离假设、执行环境声明方式 | 外部模型 ×1 次（结构化长输出）+ 验证者签名；允许可见时间压缩 |
| 1:40–2:00 | 人工批准并完成测试赔付 | 人工理由、测试回执、净损失金额 | 1 次链上交易 |
| 2:00–2:15 | 最终包存证、导出 | 包版本、链上承诺、披露范围 | 1 次链上交易 + ZIP 生成 |
| 2:15–2:45 | 独立 CLI 核验原包及 ≥1 个篡改副本 | 原包逐项通过；篡改项失败；不依赖平台后端 | ≈10 次 RPC 调用（归档端点） |
| 2:45–2:55 | 加载明确标注的拒赔对照夹具 | 用户明确授权对应的除外条款 | 本地加载 |
| 2:55–3:00 | 说明产品边界 | 中心化审核、独立可验证证据、测试赔付 | 叙述 |

上述时间是待 Spike 4 实测后修订的目标，不是性能承诺。网络等待必须可见。独立验证原包时若关键检查 Unknown，应现场解释原因，不能用绿色结果替代。展示已确认历史包时应明确标为历史验证样例，不与本次生成的最终包混用。

建议路演表述：

> 我们给能自主付款的 AI 做事故记录和处理系统。用户可以带走证据包，第三方不用登录我们的平台也能检查材料有没有被改动，并核对链上的存证。AI 帮忙调查，人来决定是否按演示规则赔付。本次使用合成事故和测试资产；审核仍由明确主体负责。

## 25. 风险与待验证假设

### 高风险假设

- Agent 支付平台愿意强制交易经过第三方 Gateway；
- 保险方会接受此类证据作为调查输入；
- 事故频率和损失规模足以支持 B2B 产品，而不是只靠现有限额与退款；
- 中立跨协议层不会快速被单一支付网络吸收；
- 关键上下文可以在不收集完整敏感内容的情况下支持归因；
- **创业部署的信任模型成立：** 演示中 Gateway、独立验证者与 AI 调用全部由本团队自托管；创业阶段若 Gateway 与验证者由买方（钱包/网关）自运行，则"独立验证者"对保险方而言不独立，保险方收到的是索赔方一侧全签的包。需要回答由谁运行验证者（本公司托管 / 保险方运行 / 双签），否则"获得可用于保险承保的证据"这一价值不成立；
- **理赔处理本身可能受牌照约束：** 计算 eligible_loss、出具批准/拒赔建议、按理赔处理收费，在部分司法辖区可能构成 claims adjusting / TPA 活动，需要牌照或必须以持牌方名义进行；"只提供证据与工作流"并不显然在监管圈外；
- **Mandate 签署摩擦：** 每个任务前要求用户 EIP-712 签署并等待存证确认，用户与钱包方是否接受该 UX 成本未验证。

### 商业上必须尽快回答

- Agent Wallet / 支付网关是否真的愿意成为第一位付费买方，而不仅是设计伙伴？
- 买方按交易量、受保护额度还是 Claim 付费？
- 现有 cyber/E&O、chargeback 和退款流程具体在哪些案例拒绝承担损失？
- 保险方决定赔付时真正需要哪十个字段？
- 对高频低额 x402 交易，应采用月度聚合保障还是只保护高价值交易？
- 验证者由谁运行、理赔处理是否需要牌照（见上）。

## 26. The Assignment

在继续扩大功能前，制作一页可展示的 Claim Packet 样例，分别交给：

- 两名保险承保或理赔从业者；
- 两家 Agent 钱包、x402 或支付平台团队。

只问三个问题（避免引导性措辞）：

1. 如果客户声称 Agent 转错了钱，这些证据够不够你决定下一步？
2. 哪个字段没有价值，哪个关键字段缺失？
3. 你现在处理一起这类争议需要多长时间、经过哪些系统？如果这个流程改变了其中哪一步，谁会为它付钱？

把回答原话记录下来。至少一家 Wallet / 支付网关愿意继续提供测试工作流或讨论接入，且至少一名保险从业者认可其能进入案件调查，才把"保险理赔基础设施"视为获得了第一层创业验证。

---

## 27. 当前结论

Agent Flight Recorder 的黑客松版本不是一家保险公司，而是一个可运行的“授权—事故—证据—AI 理赔—人工批准—测试赔付”闭环。

它的创业版本也不是逐笔微型保险，而是为 Agent 支付平台和保险机构提供协议无关的事故证据与恢复连接基础设施（恢复由持牌方或测试合约执行，本项目不持有真实资金）。Mastercard VI、AP2、x402 与现有观测工具不是需要重造的竞争底座，而是 Claim Packet 的上游证据来源。

本项目最需要保护的产品纪律是：

> 不承诺阻止一切事故，不承诺 AI 自动定责，不承诺无牌照赔付；只承诺将一次原本说不清的 Agent 支付事故，转换成可验证、可审核、可执行恢复的案件。

## 28. v1.3 对应 AIX 的交付检查

| AIX 要求 | v1.3 对应交付 | 验收边界 |
|---|---|---|
| 可运行核心路径 | 授权、攻击、支付、验证、分析、审核、测试赔付和证据导出 | 代码、复现步骤、真实运行与失败记录 |
| AI 实质参与 | 分析硬字段正常但任务语义偏离的事故 | 至少 6 个固定案件；通过 Story 4.3 正向/反向/稳定性门槛；评测器为确定性程序 |
| C 方向关键步骤可验证 | 确定性验证与 AI 输入输出摘要的独立签名声明、链上承诺；硬件 TEE 为 P1 | 不声称证明外部模型内部推理或结论正确；不声称软件级声明等同硬件 attestation 或执行证明 |
| 独立可复核报告 | 版本化 ZIP 与开源 CLI | 平台后端断开后可验证已持有包；链检查仍需 RPC |
| 隐私和边界 | 最小披露、受控导出、带盐承诺、测试资产 | 公开版部分可验不等于完整案卷可验 |
| 演示真实性 | 原包通过、篡改失败、拒赔对照、明确人工节点 | 披露历史包、可见时间压缩、网络及外部模型依赖 |

提交包补充验证器源码、独立验证使用说明、固定算法测试向量、随 CLI 仓库发布的信任配置及其取得方式、篡改负例生成脚本与 N01–N11 样本、6 个夹具与 ground-truth、评测器与评测记录、字段映射表（x402 / AP2 / VI 参考）、EvidenceAnchor 与 ClaimVault 合约地址和部署配置、AFR-TEST-USD 代币地址，以及 §20 四个解封 Spike 的结果记录。记录包体积、导出耗时、离线和在线验证耗时、存证确认耗时及费用；不虚构性能数字。

AIX 所附执行版列出的评分权重合计为 95%，与总分 100 分不一致，须向主办方核对（责任人：提交负责人；时限：正式提交前）；不得自行补齐权重。本次修订不改变所有能力尚未实现、尚未验证的状态。
