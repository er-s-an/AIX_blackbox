# Optional private source for: AFR_SOURCE_ENV=/absolute/path/to/.env pnpm setup:local
# Do not commit the filled file. Generated service credentials live in .secrets/.
# Funded Injective EVM TESTNET deployer; never use a mainnet production wallet.
DEPLOYER_PK=
AI_BASE_URL=
AI_API_KEY=
AI_MODEL=
AI_MODEL_AGENT=
# Chain is fixed to 1439 by implementation. Live local UI: http://localhost:4310
# Existing installations preserve their .secrets/*.env and deployed contract addresses.
